(function(){var w=window.I18N;var d=w&&w['en-us'];if(!d)return;Object.assign(d,{hero:{back:"Back to Blog",category:"Design · Architecture",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team",title:"Why We Chose Pydantic v2 as ActiveRecord's Base Class",sub:"Trade-offs in type safety, validation pipeline, and IDE experience."},body:[{tag:"lead",html:"When designing and implementing rhosocial ActiveRecord, we faced a critical decision: what should be the base class for models? Traditional ORMs tend to use metaclasses or decorators to declare fields, but ActiveRecord chose to directly inherit Pydantic's <code>BaseModel</code>. This article explains the reasoning behind this decision, along with the capabilities and constraints it brings."},{tag:"h2",html:"Comparing the Options"},{tag:"p",html:"In the Python ORM ecosystem, model definition roughly falls into three approaches:"},{tag:"ul",items:["<strong>Metaclass approach</strong> (e.g., SQLAlchemy's <code>declarative_base</code>): intercepts class creation via metaclasses, converting class attributes into special descriptors.","<strong>Decorator approach</strong>: marks classes with decorators like <code>@model</code>, rewriting class definitions at runtime.","<strong>Inheritance approach</strong>: directly inherits from a base class with a built-in field system."]},{tag:"p",html:"The inheritance approach is the most intuitive for Python programmers. The question is: should this base class be self-built, or should we reuse an existing mature solution?"},{tag:"h2",html:"Defining Fields: Attributes Are Database Columns"},{tag:"p",html:"In rhosocial-activerecord, every type-annotated attribute on a model corresponds to a database table column. Since <code>ActiveRecord</code> directly inherits Pydantic's <code>BaseModel</code>, all fields are standard Pydantic fields—Pydantic automatically tracks their types, defaults, and validation rules:"},{tag:"code",lang:"python",text:"from rhosocial.activerecord.model import ActiveRecord\n\nclass User(ActiveRecord):\n    id:    int | None = None\n    name:  str\n    email: str\n    age:   int = 0"},{tag:"code-tabs",filename:"model.py",lang:"python",tabs:[{label:"Python 3.8",code:"from rhosocial.activerecord.model import ActiveRecord
from typing import Optional

class User(ActiveRecord):
    id:    Optional[int] = None
    name:  str
    email: str
    age:   int = 0"},{label:"Python 3.10+",code:"from rhosocial.activerecord.model import ActiveRecord

class User(ActiveRecord):
    id:    int | None = None
    name:  str
    email: str
    age:   int = 0"}]},{tag:"p",html:"Here <code>id</code>, <code>name</code>, <code>email</code>, and <code>age</code> are both Python type annotations and database table columns. Pydantic's <code>BaseModel</code> automatically records metadata about each field—its type, default value, whether it's required, and so on. rhosocial-activerecord builds on this, mapping each field to a database column."},{tag:"p",html:"This means developers don't need to learn two separate field declaration syntaxes—Pydantic's syntax is rhosocial-activerecord's declaration syntax, and vice versa. This also brings type safety, validation pipelines, and serialization out of the box."},{tag:"h2",html:"Why Not a Custom Metaclass"},{tag:"p",html:"Building a custom metaclass system means implementing from scratch:"},{tag:"ul",items:["Field type inference","Data validation pipeline (type checking, custom validators)","Serialization / deserialization (<code>model_dump</code> / <code>model_validate</code>)","JSON Schema generation","IDE type completion support"]},{tag:"p",html:"This is a massive undertaking. More importantly, the community already has mature, battle-tested libraries like Pydantic. Pydantic v2 rewrote its core validation engine in Rust, achieving 5-50x performance improvements over v1."},{tag:"callout",type:"tip",html:"<strong>Core principle:</strong> Don't reinvent the wheel unless existing wheels can't meet your needs. Pydantic v2 not only meets our needs—it does so better than any custom solution would."},{tag:"h2",html:"What Pydantic v2 Brings"},{tag:"h3",html:"1. Type Safety"},{tag:"p",html:"Pydantic v2 is fully based on Python type annotations. Field types <em>are</em> Python types, giving perfect IDE completion and type checking:"},{tag:"code",lang:"python",text:"from rhosocial.activerecord.model import ActiveRecord\n\nclass User(ActiveRecord):\n    id:    int | None = None\n    name:  str\n    email: str\n    age:   int = 0"},{tag:"p",html:"Since <code>ActiveRecord</code> directly inherits <code>BaseModel</code>, all fields are standard Pydantic fields. This means:"},{tag:"ul",items:["Pyright / mypy / PyCharm all correctly infer each field's type","Passing wrong types during instantiation gets immediate IDE feedback","<code>model_dump()</code> / <code>model_validate()</code> serialization methods work out of the box"]},{tag:"h3",html:"2. Validation Pipeline"},{tag:"p",html:"Pydantic v2's validator system allows adding validation logic at both the field and model levels. While Pydantic provides custom validators like <code>field_validator</code> and <code>model_validator</code>, in practice you should <strong>prefer Pydantic's built-in validation capabilities</strong>—such as <code>Field(ge=0)</code>, <code>Field(min_length=1)</code>, <code>Literal</code> type constraints, etc. The reason is performance: custom validators fall back to Python execution, constrained by the GIL under CPython, whereas Pydantic's built-in constraint checks are completed in the Rust core with performance differences of several times to tens of times."},{tag:"code",lang:"python",text:"# Recommended: Built-in constraints — validated in Rust core, high performance\nclass User(ActiveRecord):\n    age: int = Field(ge=0, le=150)      # Built-in range check\n    name: str = Field(min_length=1)     # Built-in length check\n    role: Literal[\"admin\", \"user\"]       # Type-level constraint\n\n# Available but use with caution: Custom validator — falls back to Python\nfrom pydantic import field_validator\n\nclass User(ActiveRecord):\n    email: str\n\n    @field_validator(\"email\")\n    @classmethod\n    def _normalize_email(cls, v: str) -> str:\n        if \"@\" not in v:\n            raise ValueError(\"invalid email\")\n        return v.lower().strip()"},{tag:"callout",html:"<strong>Performance principle:</strong> If Pydantic's built-in constraints can express it, don't write a custom validator. Built-in constraints execute in the Rust core, bypassing the Python interpreter and GIL entirely. Reserve custom validators for cases where built-in capabilities fall short—such as cross-field validation or accessing external services."},{tag:"p",html:"rhosocial-activerecord triggers a full Pydantic validation before each <code>save()</code>, ensuring data integrity before it reaches the database. This provides additional runtime guarantees on top of Python's type checking."},{tag:"h3",html:"3. Zero Additional Learning Cost"},{tag:"p",html:"If developers are already familiar with Pydantic, using rhosocial-activerecord requires no new field declaration syntax. If they're not familiar with Pydantic, what they learn about Pydantic applies equally to rhosocial-activerecord—a two-way protection on developer time investment."},{tag:"h2",html:"How to Escape BaseModel Tracking"},{tag:"p",html:"Inheriting Pydantic's <code>BaseModel</code> means every type-annotated attribute is tracked by Pydantic. But in real ORM usage, some attributes <strong>should not</strong> be tracked by Pydantic or mapped to database columns:"},{tag:"ul",items:["<strong>Relations</strong>: <code>posts: list[Post]</code> is a logical relationship derived through foreign keys, not a database column","<strong>Field Proxies</strong>: Some attributes appear as regular Pydantic fields but need special ORM behavior (lazy loading, computed properties)","<strong>Non-persistent attributes</strong>: Runtime state such as caches or temporary computed values should not be serialized or persisted"]},{tag:"p",html:"rhosocial-activerecord provides several ways to bypass or extend BaseModel's default tracking behavior:"},{tag:"h3",html:"Approach 1: Pydantic's PrivateAttr"},{tag:"p",html:"Pydantic v2 provides <code>PrivateAttr</code>. Attributes marked with this are not serialized, validated, or included in the schema by Pydantic. rhosocial-activerecord will also not map these to database columns:"},{tag:"code",lang:"python",text:"from pydantic import PrivateAttr\n\nclass User(ActiveRecord):\n    name: str\n    _cache: dict = PrivateAttr(default_factory=dict)"},{tag:"h3",html:"Approach 2: Field-Level Exclusion"},{tag:"p",html:"rhosocial-activerecord allows specifying column mapping behavior at the field declaration level. When <code>column=None</code>, the field remains a Pydantic field (participating in validation and serialization) but is not mapped to a database table column:"},{tag:"code",lang:"python",text:"class User(ActiveRecord):\n    name: str\n    computed_score: float = Field(default=0.0, column=None)"},{tag:"h3",html:"Approach 3: Field Proxy"},{tag:"p",html:"Field Proxy is rhosocial-activerecord's core extension mechanism. It allows layering custom ORM behavior—such as lazy loading, change tracking, and access interception—on top of Pydantic fields without modifying their behavior."},{tag:"p",html:"The essential purpose of Field Proxy is to let IDEs and static type checkers correctly understand field types. Through Field Proxy, <code>User.c.name</code> is recognized by the IDE as a string-typed expression object rather than an untyped attribute—enabling full autocompletion and type inference for query conditions like <code>User.c.age > 18</code>. Additionally, Field Proxy makes it natural to express ORM-specific semantics such as JOIN aliases in query construction."},{tag:"p",html:"This will be discussed in detail in a later article."},{tag:"h2",html:"Key Differences from Custom Solutions"},{tag:"p",html:"Choosing Pydantic also means giving up certain freedoms. For example, rhosocial-activerecord cannot control the internal representation of fields—reading and writing fields fully follows Pydantic's descriptor protocol. But this is not actually a limitation: the Field Proxy mechanism lets developers attach custom methods without modifying Pydantic field behavior."},{tag:"p",html:"On the other hand, Pydantic v2's Rust core means rhosocial-activerecord's model instantiation performance far exceeds pure-Python metaclass-based solutions. This is especially important for batch data processing scenarios."},{tag:"h2",html:"Summary"},{tag:"p",html:"Choosing Pydantic v2 as the model base class is ultimately a decision to stand on the shoulders of giants:"},{tag:"ul",items:["Validation and serialization go to Pydantic — it does this better than any custom ORM solution could","SQL generation and database operations go to rhosocial-activerecord — this is our area of focus","Developers just write business logic — no need to care about how the field system works internally"]},{tag:"hr"},{tag:"next",html:"Next: Fluent API — ActiveRecord.save and ActiveQuery, making code read like English."}]})})();