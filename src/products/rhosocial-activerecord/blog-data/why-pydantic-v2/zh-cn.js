(function(){var w=window.I18N;var d=w&&w['zh-cn'];if(!d)return;Object.assign(d,{hero:{back:"返回 Blog",category:"设计 · 架构",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"},author:"by rhosocial 团队",title:"为什么我们选择 Pydantic v2 作为 ActiveRecord 的基类",sub:"类型安全、验证管道与 IDE 体验的取舍。"},body:[{tag:"lead",html:"在设计和实现 rhosocial ActiveRecord 时，我们面临了一个关键决策：模型类的基类应该是什么？传统的 ORM 方案倾向于使用元类（metaclass）或装饰器来声明字段，但 ActiveRecord 选择了直接继承 Pydantic 的 <code>BaseModel</code>。这篇文章将解释这一决策背后的考量，以及由此带来的能力和约束。"},{tag:"h2",html:"选项对比"},{tag:"p",html:"在 Python ORM 生态中，模型定义大致有三种方案："},{tag:"ul",items:["<strong>元类方案</strong>（如 SQLAlchemy 的 <code>declarative_base</code>）：通过元类拦截类创建过程，将类属性转换为特殊描述符。","<strong>装饰器方案</strong>：用 <code>@model</code> 等装饰器标记类，在运行时改写类定义。","<strong>继承方案</strong>：直接继承一个带字段系统的基类。"]},{tag:"p",html:"继承方案最符合 Python 程序员的直觉。问题是：这个基类应该是 ORM 自研的，还是复用已有的成熟方案？"},{tag:"h2",html:"定义字段：属性即数据表字段"},{tag:"p",html:"在 rhosocial-activerecord 中，模型的每个类型注解属性都对应一个数据库表字段。由于 <code>ActiveRecord</code> 直接继承 Pydantic 的 <code>BaseModel</code>，所有字段都是标准的 Pydantic 字段——Pydantic 会自动跟踪这些字段的类型、默认值和验证规则："},{tag:"code",lang:"python",text:"from rhosocial.activerecord.model import ActiveRecord\n\nclass User(ActiveRecord):\n    id:    int | None = None\n    name:  str\n    email: str\n    age:   int = 0"},{tag:"code-tabs",filename:"model.py",lang:"python",tabs:[{label:"Python 3.8",code:"from rhosocial.activerecord.model import ActiveRecord
from typing import Optional

class User(ActiveRecord):
    id:    Optional[int] = None
    name:  str
    email: str
    age:   int = 0"},{label:"Python 3.10+",code:"from rhosocial.activerecord.model import ActiveRecord

class User(ActiveRecord):
    id:    int | None = None
    name:  str
    email: str
    age:   int = 0"}]},{tag:"p",html:"这里 <code>id</code>、<code>name</code>、<code>email</code>、<code>age</code> 既是 Python 类型注解，也是数据表字段。Pydantic 的 <code>BaseModel</code> 会自动记录这些字段的元信息：类型、默认值、是否必填等。rhosocial-activerecord 在此基础上，将这些字段一一映射到数据库表的列。"},{tag:"p",html:"这意味着开发者不需要学习两套字段声明语法——Pydantic 的语法就是 rhosocial-activerecord 的声明语法，反之亦然。这也带来了类型安全、验证管道和序列化的开箱即用体验。"},{tag:"h2",html:"为什么不是自研元类"},{tag:"p",html:"自研元类系统意味着我们需要自己实现："},{tag:"ul",items:["字段类型推导","数据验证管道（类型校验、自定义校验器）","序列化 / 反序列化（<code>model_dump</code> / <code>model_validate</code>）","JSON Schema 生成","IDE 类型补全支持"]},{tag:"p",html:"这是一项巨大的工程。更重要的是，社区已经有 Pydantic 这样成熟、经过大规模验证的库。Pydantic v2 用 Rust 重写了核心校验引擎，性能相比 v1 提升了 5-50 倍。"},{tag:"callout",type:"tip",html:"<strong>核心原则：</strong>不重复造轮子，除非现有轮子无法满足需求。Pydantic v2 不仅能满足需求，而且做得更好。"},{tag:"h2",html:"Pydantic v2 带来的好处"},{tag:"h3",html:"1. 类型安全"},{tag:"p",html:"Pydantic v2 完全基于 Python 类型注解。字段的类型就是 Python 类型，IDE 可以完美补全和类型检查："},{tag:"code",lang:"python",text:"from rhosocial.activerecord.model import ActiveRecord\n\nclass User(ActiveRecord):\n    id:    int | None = None\n    name:  str\n    email: str\n    age:   int = 0"},{tag:"p",html:"由于 <code>ActiveRecord</code> 直接继承 <code>BaseModel</code>，所有字段都是标准的 Pydantic 字段。这意味着："},{tag:"ul",items:["Pyright / mypy / PyCharm 都能正确推导每个字段的类型","实例化时传错类型会得到 IDE 即时反馈","<code>model_dump()</code> / <code>model_validate()</code> 等序列化方法开箱即用"]},{tag:"h3",html:"2. 验证管道"},{tag:"p",html:"Pydantic v2 的验证器系统允许在字段级别和模型级别添加校验逻辑。虽然 Pydantic 提供了 <code>field_validator</code>、<code>model_validator</code> 等自定义验证函数，但在实际使用中，<strong>应优先使用 Pydantic 内置的验证能力</strong>——如 <code>Field(ge=0)</code>、<code>Field(min_length=1)</code>、<code>Literal</code> 类型约束等。原因在于性能：自定义验证函数会回落到 Python 层执行，在 CPython 下受 GIL 约束，而 Pydantic 内置的约束检查在 Rust 核心中完成，性能差距可达数倍甚至数十倍。"},{tag:"code",lang:"python",text:"# 推荐：使用内置约束——Rust 核心验证，高性能\nclass User(ActiveRecord):\n    age: int = Field(ge=0, le=150)      # 内置范围检查\n    name: str = Field(min_length=1)     # 内置长度检查\n    role: Literal[\"admin\", \"user\"]       # 类型级约束\n\n# 可用但需谨慎：自定义验证器——回落 Python 层执行\nfrom pydantic import field_validator\n\nclass User(ActiveRecord):\n    email: str\n\n    @field_validator(\"email\")\n    @classmethod\n    def _normalize_email(cls, v: str) -> str:\n        if \"@\" not in v:\n            raise ValueError(\"invalid email\")\n        return v.lower().strip()"},{tag:"callout",html:"<strong>性能原则：</strong>能用 Pydantic 内置约束解决的，就不要写自定义验证器。内置约束在 Rust 核心中执行，不经过 Python 解释器、不受 GIL 影响。自定义验证器仅在内置能力无法表达时使用——例如需要跨字段校验、需要访问外部服务等场景。"},{tag:"p",html:"rhosocial-activerecord 在每次 <code>save()</code> 前自动触发一次完整的 Pydantic 校验，确保入库数据合法。这一层在已有的 Python 类型检查之上提供额外的运行时保障。"},{tag:"h3",html:"3. 零额外学习成本"},{tag:"p",html:"如果开发者已经熟悉 Pydantic，使用 rhosocial-activerecord 不需要学习新的字段声明语法。如果开发者不熟悉 Pydantic，他们学到的 Pydantic 知识同样适用于 rhosocial-activerecord——这是对开发者时间投资的双向保护。"},{tag:"h2",html:"如何避开 BaseModel 跟踪"},{tag:"p",html:"继承 Pydantic 的 <code>BaseModel</code> 意味着所有带有类型注解的属性都会被 Pydantic 跟踪。但在实际的 ORM 使用中，有些属性<strong>不应该</strong>被 Pydantic 跟踪或映射到数据库字段："},{tag:"ul",items:["<strong>关联关系</strong>：<code>posts: list[Post]</code> 这种关系属性不是数据库字段，而是通过外键查询得出的逻辑关系","<strong>字段代理</strong>：有些属性在 Pydantic 层面是普通字段，但在 ORM 层面需要特殊行为（如延迟加载、计算属性）","<strong>非持久化属性</strong>：一些运行时状态（如缓存、临时计算值）不应该被序列化或持久化"]},{tag:"p",html:"rhosocial-activerecord 提供以下几种方式来避开或扩展 BaseModel 的默认跟踪行为："},{tag:"h3",html:"方式一：Pydantic 的 PrivateAttr"},{tag:"p",html:"Pydantic v2 提供了 <code>PrivateAttr</code>，带此标记的属性不会被 Pydantic 序列化、验证或纳入 schema 中。rhosocial-activerecord 也不会将这些属性映射为数据库字段："},{tag:"code",lang:"python",text:"from pydantic import PrivateAttr\n\nclass User(ActiveRecord):\n    name: str\n    _cache: dict = PrivateAttr(default_factory=dict)"},{tag:"h3",html:"方式二：字段级排除"},{tag:"p",html:"rhosocial-activerecord 允许在字段声明时指定数据库列映射行为。当 <code>column=None</code> 时，该字段虽然是 Pydantic 字段（参与验证和序列化），但不会被映射到数据库表列："},{tag:"code",lang:"python",text:"class User(ActiveRecord):\n    name: str\n    computed_score: float = Field(default=0.0, column=None)  # Pydantic 管理，但非数据库字段"},{tag:"h3",html:"方式三：字段代理"},{tag:"p",html:"字段代理（Field Proxy）是 rhosocial-activerecord 的核心扩展机制。它允许在不修改 Pydantic 字段行为的前提下，在 ORM 层面叠加自定义行为——比如延迟加载、变更追踪、访问拦截等。"},{tag:"p",html:"字段代理的本质目的是让 IDE 和静态类型检查器正确理解字段的类型。通过字段代理，<code>User.c.name</code> 在 IDE 中会被识别为字符串类型的表达式对象，而非一个无类型的属性——这让 <code>User.c.age > 18</code> 这样的查询条件能获得完整的自动补全和类型推导。同时，字段代理也方便表达连接别名（JOIN alias）等 ORM 特有的语义，使查询构建更加自然。"},{tag:"p",html:"这部分将在后续文章中详细讨论。"},{tag:"h2",html:"与自研方案的关键差异"},{tag:"p",html:"选择 Pydantic 也意味着放弃了某些自由度。例如，rhosocial-activerecord 不能控制字段的内部表示方式——字段的读取和写入完全遵循 Pydantic 的描述符协议。但这其实不是限制：字段代理（Field Proxy）机制让开发者可以在不修改 Pydantic 字段行为的前提下挂载自定义方法。"},{tag:"p",html:"另一方面，Pydantic v2 的 Rust 核心意味着 rhosocial-activerecord 的模型实例化性能远超基于纯 Python 元类的方案。对于批量数据处理场景，这一点尤为重要。"},{tag:"h2",html:"总结"},{tag:"p",html:"选择 Pydantic v2 作为模型基类，本质上是一个\"站在巨人的肩膀上\"的决策："},{tag:"ul",items:["验证和序列化交给 Pydantic——它做得比任何 ORM 自研方案都好","SQL 生成和数据库操作交给 rhosocial-activerecord——这是我们专注的领域","开发者只写业务代码——不用关心字段系统内部如何工作"]},{tag:"hr"},{tag:"next",html:"下一篇：流式 API——ActiveRecord.save 与 ActiveQuery，让代码读起来像英语。"}]})})();