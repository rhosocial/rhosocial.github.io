(function(){var w=window.I18N;var d=w&&w["en-us"];if(!d)return;Object.assign(d,{hero:{category:"Relation · Design",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team"},body:[{ tag: "lead", html: "Relation mapping is one of the core capabilities of any ORM. rhosocial-activerecord provides declarative relationship definitions like <code>belongs_to</code> and <code>has_many</code>, along with flexible loading strategies. This article introduces rhosocial-activerecord's relation mapping system in detail." },{ tag: "h2", html: "Declaring Relations" },{ tag: "p", html: "rhosocial-activerecord's relation declarations live inside model classes, defining associations with other models using declarative syntax:" },{ tag: "code", text: "from rhosocial.activerecord import ActiveRecord, belongs_to, has_many\n\nclass User(ActiveRecord):\n    id: int | None = None\n    name: str\n    email: str\n\n    # Relation declaration—not a Pydantic field\n    posts: list[Post] = has_many()\n\nclass Post(ActiveRecord):\n    id: int | None = None\n    title: str\n    content: str\n    user_id: int | None = None\n\n    # Relation declaration\n    author: User | None = belongs_to()" },{ tag: "p", html: "Key insight: Relation declarations are NOT Pydantic fields—they are not tracked by Pydantic's <code>BaseModel</code> and do not participate in serialization or validation. They are purely ORM-level logical declarations that establish associations between models." },{ tag: "h2", html: "Relation Types" },{ tag: "p", html: "rhosocial-activerecord currently supports the following relation types:" },{ tag: "ul", items: ["<strong>belongs_to</strong>: The \"many\" side of a many-to-one relationship. The model holds the foreign key (e.g., <code>Post.user_id</code>). After declaration, <code>post.author</code> accesses the associated User.","<strong>has_many</strong>: The \"one\" side of a one-to-many relationship. Reversely queries all associated models via the foreign key. After declaration, <code>user.posts</code> accesses all associated Post records.","<strong>has_one</strong>: A one-to-one relationship. Similar to has_many but limited to returning a single record."]},{ tag: "p", html: "Relation names can be customized but default to the lowercase+underscore form of the target model class name." },{ tag: "h2", html: "Relation Parameters" },{ tag: "p", html: "Relation declarations support various parameters to customize behavior:" },{ tag: "code", text: "class Post(ActiveRecord):\n    # Custom foreign key\n    author: User | None = belongs_to(foreign_key=\"author_id\")\n\nclass User(ActiveRecord):\n    # Custom sorting\n    posts: list[Post] = has_many(order_by=\"-created_at\")\n\n    # Conditional filtering\n    active_posts: list[Post] = has_many(\n        foreign_key=\"user_id\",\n        where={\"status\": \"published\"}\n    )" },{ tag: "h2", html: "Loading Strategies" },{ tag: "p", html: "rhosocial-activerecord supports two relation loading strategies, allowing developers to choose the right approach for each scenario:" },{ tag: "h3", html: "Lazy Loading" },{ tag: "p", html: "The default strategy. Related objects are loaded from the database only when first accessed. If the relation property is never accessed, no additional database query is executed." },{ tag: "code", text: "# Lazy loading: query executes only on access\npost = Post.query().first()\nprint(post.author.name)  # SELECT ... FROM users WHERE id = ? happens here" },{ tag: "p", html: "Lazy loading's advantage is simplicity and on-demand loading. Its downside is the N+1 query problem—iterating over N posts and accessing each author triggers N+1 queries." },{ tag: "h3", html: "Eager Loading" },{ tag: "p", html: "Use ActiveQuery's <code>with_()</code> method to proactively load related data and avoid N+1:" },{ tag: "code", text: "# Eager loading: batch load all relations in one query\nusers = User.query().with_(\"posts\").all()\nfor user in users:\n    print(user.name, len(user.posts))  # No extra queries" },{ tag: "p", html: "Eager loading is suitable for list views, data exports, and any scenario where a large amount of related data needs to be accessed. rhosocial-activerecord's eager loading implementation generates efficient JOIN or WHERE IN queries." },{ tag: "h2", html: "Relation-Pydantic Synergy" },{ tag: "p", html: "As mentioned, relation declarations are not Pydantic fields. This means:" },{ tag: "ul", items: ["<code>model_dump()</code> does not include relation data by default—avoiding deep nested serialization and circular references","Relation properties are not subject to Pydantic validation—no need to define validation rules for related models","Relations can be lazy-loaded—Pydantic field validation happens immediately at model initialization, while relations load on demand"]},{ tag: "p", html: "This design maintains separation of concerns: Pydantic manages data integrity, rhosocial-activerecord manages data relationships." },{ tag: "h2", html: "Integration with ActiveQuery" },{ tag: "p", html: "Relations also serve as entry points for queries. Using relation properties, you can query in reverse from the associated model:" },{ tag: "code", text: "# Find Python articles published by a specific user\nuser = User.query().where(name=\"Alice\").first()\npython_posts = user.posts.query().where(title__contains=\"Python\").all()" },{ tag: "p", html: "Here, <code>user.posts.query()</code> returns a pre-filtered ActiveQuery instance that only queries Posts belonging to that User. This provides a natural way to start queries from an association." },{ tag: "h2", html: "Summary" },{ tag: "p", html: "rhosocial-activerecord's relation mapping system design principles:" },{ tag: "ul", items: ["Relation declarations are pure ORM logic, not interfering with Pydantic's field management","Both lazy loading and eager loading are available for developers to choose from","Relations integrate seamlessly with the query system, supporting complex queries starting from associations"]},{ tag: "hr" },{ tag: "next", html: "Next: Why we need Field Proxy—layering ORM-specific custom behaviors on top of Pydantic." }]})})();