(function(){var w=window.I18N;var d=w&&w["zh-cn"];if(!d)return;Object.assign(d,{hero:{back: "返回 Blog",category:"关系 · 设计",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"},title: "关系映射：声明方式与加载策略",sub: "belongs_to / has_many 的关系定义，eager/lazy loading 的策略权衡。"},body:[{ tag: "p", html: "<strong>2026-05-15</strong> · by rhosocial team" },{ tag: "lead", html: "关系映射是 ORM 的核心能力之一。AR 提供了 <code>belongs_to</code>、<code>has_many</code> 等声明式关系定义，以及灵活的关系加载策略。这篇文章将详细介绍 AR 的关系映射系统。" },{ tag: "h2", html: "关系声明" },{ tag: "p", html: "AR 的关系声明位于模型类内部，使用声明式语法定义与其他模型的关联：" },{ tag: "code", text: "from rhosocial.activerecord import ActiveRecord, belongs_to, has_many\n\nclass User(ActiveRecord):\n    id: int | None = None\n    name: str\n    email: str\n\n    # 关系声明——这不是 Pydantic 字段\n    posts: list[Post] = has_many()\n\nclass Post(ActiveRecord):\n    id: int | None = None\n    title: str\n    content: str\n    user_id: int | None = None\n\n    # 关系声明\n    author: User | None = belongs_to()" },{ tag: "p", html: "关键思想：关系声明不是 Pydantic 字段——它们不会被 Pydantic 的 <code>BaseModel</code> 跟踪，不会参与序列化和验证。它们是纯 ORM 层面的逻辑声明，用于在模型之间建立关联。" },{ tag: "h2", html: "关系类型" },{ tag: "p", html: "AR 当前支持以下关系类型：" },{ tag: "ul", items: ["<strong>belongs_to</strong>：多对一关系的\"多\"方。模型持有外键（如 <code>Post.user_id</code>）。声明后可以通过 <code>post.author</code> 访问关联的 User。","<strong>has_many</strong>：一对多关系的\"一\"方。通过外键反向查询所有关联模型。声明后可以通过 <code>user.posts</code> 访问所有关联的 Post。","<strong>has_one</strong>：一对一关系。类似 has_many，但限定只返回一条记录。"]},{ tag: "p", html: "关系名称可以自定义，默认从目标模型类名推导。例如 <code>belongs_to()</code> 默认使用目标模型类名的小写+下划线形式作为关系名称。" },{ tag: "h2", html: "关系参数" },{ tag: "p", html: "关系声明支持多种参数来自定义行为：" },{ tag: "code", text: "class Post(ActiveRecord):\n    # 自定义外键\n    author: User | None = belongs_to(foreign_key=\"author_id\")\n\nclass User(ActiveRecord):\n    # 自定义排序\n    posts: list[Post] = has_many(order_by=\"-created_at\")\n\n    # 条件过滤\n    active_posts: list[Post] = has_many(\n        foreign_key=\"user_id\",\n        where={\"status\": \"published\"}\n    )" },{ tag: "h2", html: "加载策略" },{ tag: "p", html: "AR 支持两种关系加载策略，开发者可以根据场景灵活选择：" },{ tag: "h3", html: "延迟加载（Lazy Loading）" },{ tag: "p", html: "默认策略。关系对象在首次访问时才从数据库加载。如果从未访问关系属性，则不执行额外的数据库查询。" },{ tag: "code", text: "# 延迟加载：直到访问 post.author 才执行查询\npost = Post.query().first()\nprint(post.author.name)  # 此时执行 SELECT ... FROM users WHERE id = ?" },{ tag: "p", html: "延迟加载的优势是简单、按需加载。缺点是 N+1 查询问题——如果循环访问 N 个 Post 的 author，会产生 N+1 次查询。" },{ tag: "h3", html: "预加载（Eager Loading）" },{ tag: "p", html: "通过 ActiveQuery 的 <code>with_()</code> 方法主动预加载关联数据，避免 N+1 问题：" },{ tag: "code", text: "# 预加载：一次查询用户，批量加载关联文章\nusers = User.query().with_(\"posts\").all()\nfor user in users:\n    print(user.name, len(user.posts))  # 不产生额外查询" },{ tag: "p", html: "预加载适用于列表展示、数据导出等需要访问大量关联数据的场景。AR 的预加载实现会生成高效的 JOIN 或 WHERE IN 查询。" },{ tag: "h2", html: "关系与 Pydantic 的协同" },{ tag: "p", html: "如前所述，关系声明不是 Pydantic 字段。这意味着：" },{ tag: "ul", items: ["<code>model_dump()</code> 默认不会包含关系数据——避免了深层嵌套序列化和循环引用","关系属性不会参与 Pydantic 的验证——不需要为关联模型定义验证规则","关系属性可以延迟加载——Pydantic 的字段验证会在模型初始化时立即发生，而关系可以按需加载"]},{ tag: "p", html: "这种设计保持了关注点分离：Pydantic 管理数据完整性，AR 管理数据关联。" },{ tag: "h2", html: "与 ActiveQuery 的集成" },{ tag: "p", html: "关系也可以作为查询的起点。通过关系属性，可以从关联模型反向查询：" },{ tag: "code", text: "# 查找某个用户发布的、标题包含 Python 的文章\nuser = User.query().where(name=\"Alice\").first()\npython_posts = user.posts.query().where(title__contains=\"Python\").all()" },{ tag: "p", html: "这里的 <code>user.posts.query()</code> 返回一个已预过滤的 ActiveQuery 实例，只查询属于该用户的 Post。这提供了从关联切入查询的自然方式。" },{ tag: "h2", html: "总结" },{ tag: "p", html: "AR 的关系映射系统设计原则：" },{ tag: "ul", items: ["关系声明是纯 ORM 逻辑，不干扰 Pydantic 的字段管理","延迟加载和预加载两种策略供开发者按需选择","关系与查询系统无缝集成，支持从关联出发的复杂查询"]},{ tag: "hr" },{ tag: "next", html: "下一篇：为什么需要字段代理——在 Pydantic 之上叠加 ORM 特有的自定义行为。" }]})})();