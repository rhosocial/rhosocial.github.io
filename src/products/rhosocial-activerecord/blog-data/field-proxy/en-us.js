(function(){var w=window.I18N;var d=w&&w["en-us"];if(!d)return;Object.assign(d,{hero:{back:"返回 Blog",category:"Architecture · Fields",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team",title:"Why Field Proxy: From Pydantic Types to ORM Query Expressions",sub:"Pydantic fields are powerful, but ORM queries need expressions, not values."},body:[{tag:"lead",html:"Field Proxy is one of the core designs in rhosocial-activerecord. It lets <code>ActiveRecord</code> inherit both Pydantic's type-safe field system and the ORM's query expression capability—neither abandoning Pydantic nor monkey-patching it."},
{tag:"h2",html:"What Pydantic Gave Us"},
{tag:"p",html:"Before Pydantic v2, Python ORMs declared fields either as standalone <code>Column()</code> objects (SQLAlchemy) or through decorator-generated <code>CharField()</code> calls (Django ORM). Type annotations existed in Python—IDEs could autocomplete them, mypy could check them—but they had nothing to do with \"database columns\"."},
{tag:"p",html:"Pydantic changed that: type annotations <em>are</em> field declarations. <code>name: str</code> is simultaneously a Python type hint, a validation rule, and the source of JSON Schema. rhosocial-activerecord pushes this further: <strong>field type annotations are also database column definitions</strong>."},
{tag:"code",lang:"python",text:"from pydantic import BaseModel\n\nclass User(BaseModel):\n    id: int | None = None\n    name: str\n    email: str\n    age: int = 0"},
{tag:"p",html:"This expresses \"a user has four attributes—id, name, email, age—with types …\" perfectly. The IDE knows <code>user.age</code> is <code>int</code>; mypy catches <code>user.age = \"abc\"</code>. Everything is great—<strong>until you need to write a query condition</strong>."},
{tag:"h2",html:"The Trap: User.age > 18 Is Not a Query Condition"},
{tag:"p",html:"Suppose you want to query users older than 18. The intuitive way: <code>User.age > 18</code>. But <code>User.age</code> is <code>int</code> in Pydantic, and <code>></code> triggers Python's native <code>int.__gt__</code>—returning <code>bool</code>, not a query expression."},
{tag:"code",lang:"python",text:"# This looks like a query condition, but…\nUser.age > 18  # Returns True or False (bool!)\n\n# When passed to .where():\nUser.query().where(User.age > 18)\n# → SELECT * FROM users WHERE 1  (matches ALL rows!)"},
{tag:"p",html:"The problem is subtle: <code>User.age > 18</code> evaluates to <code>True</code> immediately, and <code>.where(True)</code> silently matches every row. No error, no warning—just wrong results."},
{tag:"callout",type:"warning",html:"<strong>Root cause</strong>: Python's <code>int.__gt__</code> returns <code>bool</code>, not a query expression. ORMs need expressions, not values—and Python's type system cannot distinguish them through ordinary fields."},
{tag:"h2",html:"Why Not SQLAlchemy's Approach"},
{tag:"p",html:"SQLAlchemy faced this problem long ago. Its solution: <strong>don't use native Python types for fields; use custom descriptors</strong>."},
{tag:"code",lang:"python",text:"# SQLAlchemy's approach\nfrom sqlalchemy import Column, Integer, String\nfrom sqlalchemy.orm import declarative_base\n\nBase = declarative_base()\n\nclass User(Base):\n    __tablename__ = \"users\"\n    id = Column(Integer, primary_key=True)\n    name = Column(String)\n    age = Column(Integer)\n\n# Query:\nUser.age >= 18  # Returns BinaryExpression (query condition)"},
{tag:"p",html:"SQLAlchemy uses a metaclass (<code>DeclarativeMeta</code>) to intercept class creation, replacing <code>Column</code> with <code>InstrumentedAttribute</code> descriptors. These descriptors override <code>__gt__</code> / <code>__ge__</code> to return expression nodes, not <code>bool</code>."},
{tag:"p",html:"But <strong>Pydantic's <code>BaseModel</code> has its own metaclass</strong> (<code>ModelMetaclass</code>), which collects <code>model_fields</code>, generates validation logic, and builds JSON Schema. Python does not support multiple metaclass inheritance—you cannot have both."},
{tag:"p",html:"rhosocial-activerecord chose Pydantic's metaclass route (preserving type safety, validation, and serialization) without monkey-patching Pydantic. What we needed was: <strong>a mechanism that intercepts class-level attribute access without modifying Pydantic fields</strong>."},
{tag:"h2",html:"FieldProxy: ClassVar + Python Descriptor"},
{tag:"p",html:"The solution is surprisingly simple: <strong>declare a descriptor with <code>ClassVar</code> and place it on the class</strong>. <code>ClassVar</code> tells Pydantic \"this is not a model field, don't track it\"; the descriptor's <code>__get__</code> returns a query expression when accessed at the class level."},
{tag:"code",lang:"python",filename:"model.py",text:"from rhosocial.activerecord.model import ActiveRecord\nfrom rhosocial.activerecord.base import FieldProxy\nfrom typing import ClassVar\n\nclass User(ActiveRecord):\n    __table_name__ = \"users\"\n    id: int | None = None\n    name: str\n    email: str\n    age: int = 0\n\n    # ClassVar prevents Pydantic from treating this as a model field\n    c: ClassVar[FieldProxy] = FieldProxy()\n\n# Class-level access → Column expression\nUser.c.age >= 18  # → ComparisonPredicate\n#                    → WHERE \"users\".\"age\" >= ?"},
{tag:"p",html:"Mechanism breakdown:"},
{tag:"ul",items:["<strong><code>ClassVar</code></strong>: A Python <code>typing</code> marker. Pydantic's <code>ModelMetaclass</code> skips attributes annotated with <code>ClassVar</code>—<code>c</code> will not appear in <code>model_fields</code>, be serialized, validated, or treated as a database column","<strong><code>FieldProxy</code> descriptor</strong>: Implements Python's descriptor protocol (<code>__get__</code>). Accessing <code>User.c</code> returns a <code>_FieldAccessor</code> instance whose <code>__getattr__</code> intercepts subsequent property access (e.g., <code>.age</code>) to construct a <code>Column</code> expression node","<strong>Expression node</strong>: <code>Column</code> inherits <code>ComparisonMixin</code>, overriding <code>__ge__</code> / <code>__eq__</code> etc. to return <code>ComparisonPredicate</code> (expression tree nodes) instead of <code>bool</code>"]},
{tag:"blockquote",html:"<strong>Design principle</strong>: Pydantic handles field values and validation; FieldProxy handles field query expressions. No conflict, no overlap, no monkey-patching."},
{tag:"h2",html:"Core Capabilities of FieldProxy"},
{tag:"p",html:"Query expression is the core problem FieldProxy solves—making <code>User.c.age >= 18</code> return an expression instead of a boolean. But FieldProxy also handles column name mapping, table aliases, and column aliases for advanced query scenarios."},
{tag:"h3",html:"Query Expression"},
{tag:"p",html:"This is the core responsibility. <code>User.c.age >= 18</code> does not return <code>True</code>—it returns a <code>ComparisonPredicate</code>, an expression tree node. Expression nodes also support <code>.to_sql()</code>, allowing you to inspect the generated SQL and parameters at any time:"},
{tag:"code-result",filename:"query_expr.py",code:"from rhosocial.activerecord.model import ActiveRecord\nfrom rhosocial.activerecord.base import FieldProxy\nfrom typing import ClassVar, Optional\n\nclass User(ActiveRecord):\n    __table_name__ = \"users\"\n    id: Optional[int] = None\n    name: str\n    age: int = 0\n    c: ClassVar[FieldProxy] = FieldProxy()\n\n# User.c.age >= 18 is a ComparisonPredicate expression node\npredicate = User.c.age >= 18\n\n# Expression nodes support to_sql() directly\npredicate.to_sql()",sql:"WHERE \"users\".\"age\" >= ?\n-- Params: (18,)",result:null},
{tag:"p",html:"Notice that the value <code>18</code> is not concatenated into the SQL string—it is passed via a <code>?</code> placeholder with a separate parameters list. This follows PEP 249 (Python Database API Specification v2.0)—<strong>inherently immune to SQL injection</strong>, because user input can never become part of the SQL syntax."},
{tag:"h3",html:"Column Name Mapping (UseColumn)"},
{tag:"p",html:"Python field names and database column names don't always match. Legacy databases may use <code>camelCase</code> or specific abbreviations. FieldProxy supports custom column name mappings via the <code>UseColumn</code> annotation, transparent at the query expression level."},
{tag:"code-result",filename:"model.py",code:"from typing import Annotated\nfrom rhosocial.activerecord.model import ActiveRecord\nfrom rhosocial.activerecord.base import UseColumn, FieldProxy\nfrom typing import ClassVar, Optional\n\nclass User(ActiveRecord):\n    __table_name__ = \"users\"\n    id: Optional[int] = None\n    name: str\n    email_address: Annotated[str, UseColumn(\"email\")]\n    is_active: Annotated[bool, UseColumn(\"is_active_flag\")]\n    c: ClassVar[FieldProxy] = FieldProxy()\n\nUser.query().where(\n    User.c.email_address == \"alice@example.com\"\n).where(User.c.is_active == True).to_sql()",sql:"SELECT * FROM \"users\"\nWHERE \"email\" = ?\n  AND \"is_active_flag\" = ?",result:null},
{tag:"p",html:"The <code>UseColumn(\"email\")</code> annotation on <code>email_address</code> tells FieldProxy: the Python field is <code>email_address</code>, but the database column is <code>email</code>. Query code always uses the Python field name; column name mapping is handled transparently by FieldProxy."},
{tag:"h3",html:"Table Aliases & Column Aliases—Self-Joins"},
{tag:"p",html:"<code>with_table_alias()</code> creates table alias instances, and <code>.as_()</code> assigns column aliases. Combined, self-join queries become intuitive:"},
{tag:"code-result",filename:"self_join.py",code:"from rhosocial.activerecord.model import ActiveRecord\nfrom rhosocial.activerecord.base import FieldProxy\nfrom typing import ClassVar, Optional\n\nclass User(ActiveRecord):\n    __table_name__ = \"users\"\n    id: Optional[int] = None\n    name: str\n    manager_id: Optional[int] = None\n    c: ClassVar[FieldProxy] = FieldProxy()\n\n# Table alias\nManager = User.c.with_table_alias(\"manager\")\n\nUser.query().select(\n    User.c.name.as_(\"employee\"),\n    Manager.name.as_(\"manager_name\")\n).join(User,\n    on=(User.c.manager_id == Manager.id),\n    alias=\"manager\"\n).where(\n    Manager.name.is_not_null()\n).to_sql()",sql:"SELECT \"users\".\"name\" AS \"employee\",\n       \"manager\".\"name\" AS \"manager_name\"\nFROM \"users\"\nJOIN \"users\" AS \"manager\"\n  ON \"users\".\"manager_id\" = \"manager\".\"id\"\nWHERE \"manager\".\"name\" IS NOT NULL",result:null},
{tag:"h2",html:"IDE Support & Static Checking (Planned)"},
{tag:"p",html:"Currently, <code>User.c.age</code> appears as type <code>FieldProxy</code> in IDEs (derived from <code>ClassVar</code>), not the <code>Column</code> expression type developers expect. This means:"},
{tag:"ul",items:["IDEs cannot autocomplete field names after <code>.c</code>","mypy cannot catch <code>User.c.non_existent_field</code> type errors","Renaming a field does not update <code>.c</code> references"]},
{tag:"p",html:"The planned solution: a <strong><code>generate-stubs</code> CLI command</strong> that generates <code>.pyi</code> stub files for each model, explicitly declaring the type signatures for every field proxy. If a model has multiple field proxies (e.g., <code>c</code> and <code>mgr</code>), the stub file expands each one separately:"},
{tag:"code-result",filename:"generated_stubs.pyi",code:"# Auto-generated by rhosocial-activerecord generate-stubs\n\nclass User(ActiveRecord):\n    c: ClassVar[UserFieldProxy_c]\n    mgr: ClassVar[UserFieldProxy_mgr]\n\nclass UserFieldProxy_c:\n    \"\"\"Default proxy → users table\"\"\"\n    @property\n    def id(self) -> Column: ...\n    @property\n    def name(self) -> Column: ...\n    @property\n    def email(self) -> Column: ...\n    @property\n    def age(self) -> Column: ...\n\nclass UserFieldProxy_mgr:\n    \"\"\"mgr proxy → manager table (aliased)\"\"\"\n    @property\n    def id(self) -> Column: ...\n    @property\n    def name(self) -> Column: ...\n    @property\n    def email(self) -> Column: ...\n    @property\n    def age(self) -> Column: ...",result:null},
{tag:"p",html:"With stubs, <code>User.c.age</code> and <code>User.mgr.name</code> would type as <code>Column</code> (not <code>FieldProxy</code>), IDEs would autocomplete after <code>.c.</code> and <code>.mgr.</code>, and mypy would validate expression operations. This feature is in planning."},
{tag:"h2",html:"More Than One FieldProxy"},
{tag:"p",html:"The FieldProxy attribute name is not forced to be <code>c</code>—you can name it <code>fields</code>, <code>col</code>, or anything you like. More importantly, <strong>a single model can define multiple FieldProxy instances</strong>, each with different parameters:"},
{tag:"code-result",filename:"multi_proxy.py",code:"from rhosocial.activerecord.model import ActiveRecord\nfrom rhosocial.activerecord.base import FieldProxy\nfrom typing import ClassVar, Optional\n\nclass User(ActiveRecord):\n    __table_name__ = \"users\"\n    id: Optional[int] = None\n    name: str\n    manager_id: Optional[int] = None\n\n    # Default field proxy\n    c: ClassVar[FieldProxy] = FieldProxy()\n\n    # Second proxy with table alias—ideal for self-joins\n    mgr: ClassVar[FieldProxy] = FieldProxy(table_alias=\"manager\")\n\n# User.c.name  —— points to \"users\" table\n# User.mgr.name —— points to \"manager\" table (aliased)\n\nUser.query().select(\n    User.c.name.as_(\"employee\"),\n    User.mgr.name.as_(\"manager_name\")\n).join(User,\n    on=(User.c.manager_id == User.mgr.id),\n    alias=\"manager\"\n).to_sql()",sql:"SELECT \"users\".\"name\" AS \"employee\",\n       \"manager\".\"name\" AS \"manager_name\"\nFROM \"users\"\nJOIN \"users\" AS \"manager\"\n  ON \"users\".\"manager_id\" = \"manager\".\"id\"",result:null},
{tag:"p",html:"<code>User.c</code> and <code>User.mgr</code> are independent FieldProxy instances. <code>mgr</code> was created with <code>table_alias=\"manager\"</code>, so every field accessed through <code>User.mgr</code> uses <code>manager</code> as the table name. In self-join queries, this eliminates the need for extra <code>with_table_alias()</code> calls—the proxy carries its alias information from the start."},
{tag:"p",html:"This also means: <strong>if your database table happens to have a column named <code>c</code></strong>, and you want to keep <code>c</code> as the Python field name in your ActiveRecord model, simply declare the field proxy under a different name—<code>fields</code>, <code>col</code>, <code>query</code>… anything works. Pydantic won't track it, and FieldProxy doesn't require a fixed attribute name."},
{tag:"h2",html:"Summary"},
{tag:"p",html:"FieldProxy's role is clear: <strong>what Pydantic cannot do, it handles; what Pydantic can do, it leaves alone</strong>."},
{tag:"ul",items:["Query expression—the core responsibility, solving <code>User.age > 18</code> returning <code>bool</code>","When Pydantic's metaclass and SQLAlchemy's metaclass collide, <code>ClassVar</code> + descriptor carves a third path","Column name mapping—<code>UseColumn</code> decouples Python field names from database column names","Table aliases and column aliases—support for self-joins and column renaming in advanced queries","IDE stub generation is planned for future type-safety improvements"]}]})})();