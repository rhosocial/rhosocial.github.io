(function(){var w=window.I18N;var d=w&&w["zh-cn"];if(!d)return;Object.assign(d,{hero:{ back: "返回 Blog", category:"架构 · 字段",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"}, title: "为什么需要字段代理", sub: "在 Pydantic 字段之上叠加 ORM 特有的自定义行为。" },body:[{ tag: "p", html: "<strong>2026-05-16</strong> · by rhosocial team" },{ tag: "lead", html: "字段代理（Field Proxy）是 AR 最核心的扩展机制之一。它解决了 Pydantic 字段系统无法覆盖的 ORM 场景需求，让开发者可以在不修改 Pydantic 字段行为的前提下，叠加 ORM 特有的自定义行为。" },{ tag: "h2", html: "Pydantic 字段的局限性" },{ tag: "p", html: "Pydantic 的 <code>BaseModel</code> 字段系统非常强大——类型检查、验证、序列化、JSON Schema 生成。但这些能力是面向\"数据验证和传输\"场景设计的，不完全覆盖 ORM 的需求：" },{ tag: "ul", items: ["延迟加载（Lazy Loading）：ORM 字段可能包含尚未从数据库加载的数据，Pydantic 没有\"未加载\"的概念","变更追踪（Change Tracking）：ORM 需要知道哪些字段被修改了，以便生成 UPDATE 语句，Pydantic 默认不跟踪","访问控制：部分 ORM 字段需要只读保护（如主键一旦写入不可修改）、或通过 getter/setter 劫持访问","类型修饰：数据库层和应用层的类型不一定完全相同，ORACLE 的 DATE 和 Python 的 datetime 之间需要隐式转换"]},{ tag: "p", html: "字段代理的目标就是填补这些空白——让 Pydantic 做它擅长的事（类型安全、验证），让 ORM 做它需要的事（延迟加载、追踪、访问控制）。" },{ tag: "h2", html: "字段代理的工作原理" },{ tag: "p", html: "字段代理是一个包装器（Wrapper），位于 Pydantic 字段和 ORM 逻辑之间：用户看到的是字段代理，Pydantic 看到的是普通字段，ORM 看到的是可拦截的行为。" },{ tag: "code", text: "# 伪代码示意——字段代理的职责\nclass FieldProxy:\n    \"\"\"包装在 Pydantic 字段外的 ORM 逻辑层\"\"\"\n    def __get__(self, instance, owner):\n        if not self._loaded and instance:\n            self._lazy_load(instance)  # 延迟加载\n        self._track_access(instance)     # 访问追踪\n        return self._value\n\n    def __set__(self, instance, value):\n        self._track_change(instance)     # 变更追踪\n        self._value = self._convert(value)  # 类型转换\n        super().__set__(instance, self._value)" },{ tag: "h2", html: "延迟加载" },{ tag: "p", html: "延迟加载是字段代理最常见的用途。当一个模型从数据库加载时，某些字段（如大文本、JSON、Blob）可能不需要立即加载。字段代理可以延迟加载这些数据，直到它们被实际访问：" },{ tag: "code", text: "class Article(ActiveRecord):\n    id: int | None = None\n    title: str\n    # content 字段：查询时不加载，访问时按需加载\n    content: str = Field(lazy=True)\n\n# 查询所有文章——不加载 content 字段\narticles = Article.query().all()\n\n# 访问标题——content 不会加载\nprint(articles[0].title)\n\n# 访问 content——此时才从数据库加载\nprint(articles[0].content)" },{ tag: "p", html: "这在处理大字段时特别有用。例如，一篇文章的正文可能是 100KB 的 HTML——在列表页只需要标题时，延迟加载可以避免不必要的 I/O。" },{ tag: "h2", html: "变更追踪" },{ tag: "p", html: "AR 在调用 <code>save()</code> 时需要知道哪些字段发生了变更。字段代理会记录每个字段的\"原始值\"和\"当前值\"，从而生成精确的 UPDATE 语句：" },{ tag: "code", text: "user = User.query().where(id=1).first()\n# user.name 原始值 = \"Alice\"\n\nuser.name = \"Bob\"\n# 字段代理记录：name 从 \"Alice\" → \"Bob\"\n\nuser.save()\n# 生成：UPDATE users SET name = 'Bob' WHERE id = 1\n# 未变动的字段不包含在 SET 子句中" },{ tag: "p", html: "精确的变更追踪有两个好处：减少网络传输（只发送变化的字段）、减少锁冲突（只锁定变化的列）。" },{ tag: "h2", html: "类型转换" },{ tag: "p", html: "Pydantic 的类型系统和数据库的类型系统存在差异。字段代理可以在两者之间做透明的类型转换：" },{ tag: "ul", items: ["<strong>枚举</strong>：Python Enum → 数据库 VARCHAR / INTEGER，返回时转回 Enum","<strong>日期时间</strong>：Python datetime → 数据库特定格式（Oracle DATE、PostgreSQL TIMESTAMPTZ），处理时区转换","<strong>JSON</strong>：Python dict/list → JSON 字符串 / JSONB 二进制，返回时反序列化","<strong>自定义类型</strong>：开发者可以注册自己的类型转换器"]},{ tag: "h2", html: "访问控制" },{ tag: "p", html: "字段代理还支持访问控制。例如，主键字段在创建后不应被修改（只写一次），计算字段应保持只读：" },{ tag: "code", text: "class User(ActiveRecord):\n    id: int | None = None\n    name: str\n    # 只读字段——可以在查询中读取，但不允许手动设置\n    full_name: str = Field(read_only=True, computed=\"CONCAT(first_name, ' ', last_name)\")" },{ tag: "p", html: "当用户尝试修改只读字段时，AR 会在验证阶段抛出一个明确的错误。" },{ tag: "h2", html: "与 Pydantic 的协作" },{ tag: "p", html: "字段代理和 Pydantic 的协作模式是：Pydantic 负责类型检查→字段代理在 Pydantic 验证后叠加 ORM 行为。这种分层设计确保了互不干扰：" },{ tag: "ul", items: ["模型初始化时，Pydantic 先验证所有字段的类型正确性","验证通过后，字段代理记录每个字段的初始值（用于后续变更追踪）","属性访问时，字段代理决定是否延迟加载、是否变更追踪","调用 save() 时，字段代理提供变更列表给 SQL 生成器"]},{ tag: "h2", html: "总结" },{ tag: "p", html: "字段代理是 AR 连接 Pydantic 和 ORM 的桥梁。它不修改 Pydantic 的任何行为，而是在 Pydantic 之上叠加上 ORM 所需的扩展能力。这种设计让 AR 既充分利用 Pydantic 的类型系统和验证能力，又满足了 ORM 特有的延迟加载、变更追踪、访问控制等需求。" },{ tag: "hr" },{ tag: "next", html: "下一篇：ActiveRecord-后端分离架构——核心库与后端的解耦设计。" }]})})();