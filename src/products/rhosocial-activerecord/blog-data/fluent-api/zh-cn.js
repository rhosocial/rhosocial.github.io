(function(){var w=window.I18N;var d=w&&w['zh-cn'];if(!d)return;Object.assign(d,{hero:{back:"返回 Blog",category:"设计 · API",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"},tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"},title:"流式 API：ActiveRecord.save 与 ActiveQuery",sub:"让代码读起来像英语——流式 API 的设计哲学与实践。"},body:[{tag:"p",html:"<strong>2026-05-14</strong> · by rhosocial team"},{tag:"lead",html:"好的 API 应当像一门自然语言——读起来流畅，写起来自然。rhosocial ActiveRecord 的流式 API（Fluent API）正是基于这一理念设计的。<code>user.save()</code> 读作\"用户保存\"，<code>User.query().where(...).order_by(...).all()</code> 读作\"用户查询·条件·排序·全部\"。这篇文章将介绍 AR 流式 API 的设计哲学和关键实践。"},{tag:"h2",html:"ActiveRecord.save()：模型即数据源"},{tag:"p",html:"AR 最核心的 API 是模型实例上的 <code>save()</code> 方法。它将 CRUD 操作封装为模型对象上的方法调用，让代码表达意图而非实现细节："},{tag:"code",text:"# 创建\nuser = User(name=\"Alice\", email=\"alice@example.com\")\nuser.save()\n\n# 更新\nuser.name = \"Bob\"\nuser.save()\n\n# 删除\nuser.delete()"},{tag:"p",html:"这种设计让业务代码聚焦于\"做什么\"而不是\"怎么做\"。开发者不需要关心 INSERT 和 UPDATE 的 SQL 语法差异——AR 会根据模型状态自动判断：新对象执行 INSERT，已持久化对象执行 UPDATE。"},{tag:"h2",html:"批量操作"},{tag:"p",html:"除了单条记录的 save，AR 还提供批量操作 API："},{tag:"code",text:"# 批量创建\nUser.bulk_create([\n    User(name=\"Alice\"),\n    User(name=\"Bob\"),\n])\n\n# 批量更新\nUser.query().where(age__lt=18).update(status=\"minor\")\n\n# 批量删除\nUser.query().where(status=\"inactive\").delete()"},{tag:"p",html:"批量操作在单次数据库往返中完成，大幅提升性能。各后端可以利用数据库特有的批量语法（如 PostgreSQL 的 <code>INSERT ... ON CONFLICT</code>）进一步优化。"},{tag:"h2",html:"ActiveQuery：构建查询"},{tag:"p",html:"ActiveQuery 是 AR 的流式查询构建器。它的设计目标是让查询代码读起来像英语句子："},{tag:"code",text:"users = User.query()\\\n    .where(age__gte=18)\\\n    .where(status=\"active\")\\\n    .order_by(\"-created_at\")\\\n    .limit(10)\\\n    .all()"},{tag:"p",html:"每个方法都返回 <code>self</code>（或一个新的查询对象），支持链式调用。查询在 <code>.all()</code> 时才实际执行——这种延迟执行（Lazy Evaluation）的设计有两个好处："},{tag:"ul",items:["开发者可以按需组合查询条件，而不是一次性传递所有参数","AR 可以在执行前对整个查询进行优化，比如合并条件、去除冗余子句"]},{tag:"h2",html:"查询方法的读法"},{tag:"p",html:"AR 的查询方法命名遵循英语语法原则，让每个方法的调用都像自然语言的一个短语："},{tag:"ul",items:["<strong>.where(condition)</strong>——\"条件是...\"，指定过滤条件","<strong>.order_by(field)</strong>——\"按...排序\"，指定排序字段（前缀 <code>-</code> 表示降序）","<strong>.limit(n)</strong>——\"限制 n 条\"，限制返回数量","<strong>.offset(n)</strong>——\"跳过 n 条\"，分页偏移","<strong>.all()</strong>——\"全部\"，执行查询并返回所有结果","<strong>.first()</strong>——\"第一条\"，返回第一条结果或 None","<strong>.one()</strong>——\"唯一一条\"，要求恰好一条结果，否则抛出异常","<strong>.count()</strong>——\"计数\"，返回匹配记录的数量","<strong>.exists()</strong>——\"是否存在\"，返回布尔值"]},{tag:"p",html:"组合起来读：<code>User.query().where(age__gte=18).order_by(\"name\").limit(10).all()</code> = \"用户查询·年龄≥18·按姓名排序·取前10条·全部返回\"。"},{tag:"h2",html:"查询运算符"},{tag:"p",html:"AR 的查询条件通过双下划线运算符来表达语义，这也是 Django ORM 用户熟悉的方式："},{tag:"code",text:"# 精确匹配\nUser.query().where(name=\"Alice\")\n\n# 比较运算符\nUser.query().where(age__gt=18)      # >\nUser.query().where(age__gte=18)     # >=\nUser.query().where(age__lt=65)      # <\nUser.query().where(age__lte=65)     # <=\nUser.query().where(age__ne=0)       # !=\n\n# 集合运算符\nUser.query().where(name__in=[\"Alice\", \"Bob\"])\nUser.query().where(name__not_in=[\"Charlie\"])\n\n# 字符串匹配\nUser.query().where(name__contains=\"Ali\")\nUser.query().where(name__startswith=\"A\")\nUser.query().where(name__endswith=\"ce\")\n\n# 空值判断\nUser.query().where(email__isnull=True)"},{tag:"h2",html:"链式调用的组合性"},{tag:"p",html:"ActiveQuery 的链式调用支持条件的自由组合。开发者可以基于条件分支构建不同的查询变体："},{tag:"code",text:"def search_users(name=None, min_age=None, active_only=False):\n    q = User.query()\n    if name:\n        q = q.where(name__contains=name)\n    if min_age:\n        q = q.where(age__gte=min_age)\n    if active_only:\n        q = q.where(status=\"active\")\n    return q.order_by(\"name\").all()"},{tag:"p",html:"这种方式让查询构建更加灵活，避免了参数爆炸问题。每个查询条件都是可选的、可组合的。"},{tag:"h2",html:"与 Pydantic 的集成"},{tag:"p",html:"AR 的 save() 方法在执行数据库操作前会触发一次完整的 Pydantic 字段验证。这意味着即使你绕过了类型注解直接设置字段值，AR 也能在持久化前捕获数据问题。"},{tag:"p",html:"同时，AR 也支持 <code>save(validate=False)</code> 来跳过验证，适用于性能敏感或已预先验证的场景。"},{tag:"h2",html:"总结"},{tag:"p",html:"AR 的流式 API 设计遵循三个原则："},{tag:"ul",items:["<strong>可读性优先</strong>：代码应当像英语一样可以朗读，而不是需要心理解析的符号序列","<strong>延迟执行</strong>：查询构建和执行分离，给优化留出空间","<strong>可组合性</strong>：查询条件是自由组合的积木，不是捆绑的参数列表"]},{tag:"p",html:"这些原则让 AR 的 API 既直观又灵活，适合从快速原型到生产级应用的各类场景。"},{tag:"hr"},{tag:"next",html:"下一篇：关系映射——belongs_to / has_many 的声明式定义与加载策略。"}]})})();