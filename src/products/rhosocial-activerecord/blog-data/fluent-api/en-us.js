(function(){var w=window.I18N;var d=w&&w["en-us"];if(!d)return;Object.assign(d,{hero:{back:"返回 Blog",category:"Design · API",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team",title:"Fluent API: ActiveRecord and ActiveQuery",sub:"Making code read like English—the design philosophy and practice of fluent APIs."},body:[{tag:"lead",html:"A good API should read like a natural language—fluent to read, natural to write. rhosocial ActiveRecord's fluent API is designed around this principle. <code>user.save()</code> reads as \"user saves\", <code>User.query().where(...).order_by(...).all()</code> reads as \"query users where… order by… all\". This article introduces the design philosophy and key practices."},
{tag:"h2",html:"ActiveRecord.save() and .delete(): Model as Data Source"},
{tag:"p",html:"The core APIs are the <code>save()</code> and <code>delete()</code> instance methods on model objects. CRUD operations become method calls, letting code express intent rather than implementation details:"},
{tag:"code",lang:"python",text:"# Create\nuser = User(name=\"Alice\", email=\"alice@example.com\")\nuser.save()\n\n# Update\nuser.name = \"Bob\"\nuser.save()\n\n# Delete\nuser = User.find_one(1)\nuser.delete()"},
{tag:"p",html:"<code>save()</code> automatically determines INSERT or UPDATE via <code>is_new_record</code>—new instances INSERT, persisted instances UPDATE. UPDATE <strong>only includes dirty fields</strong> (<code>dirty_fields</code>)—columns modified since the last persistence—rather than blindly SETting every column."},
{tag:"p",html:"Certain scenarios require <strong>always updating specific fields</strong>. For example, <code>TimestampMixin</code> hooks <code>BEFORE_UPDATE</code> to set <code>updated_at</code> to the current time on every update, regardless of whether the field is marked dirty."},
{tag:"p",html:"<code>delete()</code> removes the record from the database. Both return the number of affected rows (<code>int</code>)."},
{tag:"p",html:"Look up records via class methods <code>find_one()</code>, <code>find_all()</code>, <code>find_one_or_fail()</code>, or build complex queries with ActiveQuery."},
{tag:"h2",html:"ActiveQuery: Building Queries"},
{tag:"p",html:"ActiveQuery is the fluent query builder. Each method returns <code>self</code>, enabling chaining. Queries execute only when a terminal method (e.g., <code>.all()</code>, <code>.one()</code>) is called—Lazy Evaluation:"},
{tag:"code",lang:"python",text:"users = User.query()\\\n    .where(User.c.age >= 18)\\\n    .where(User.c.status == \"active\")\\\n    .order_by((User.c.name, \"ASC\"))\\\n    .limit(10)\\\n    .all()"},
{tag:"p",html:"Lazy evaluation offers three benefits:"},
{tag:"ul",items:["Combine conditions incrementally without passing all parameters at once","ActiveQuery can optimize the entire query before execution (e.g., merge conditions, remove redundant clauses)","Call <code>.to_sql()</code> at any time to inspect the generated SQL and parameters without executing"]},
{tag:"h2",html:"Terminal Methods"},
{tag:"p",html:"Terminal methods execute the query and return results:"},
{tag:"ul",items:["<strong><code>.all()</code></strong>—return all matching records (<code>List[Model]</code>), empty list if none","<strong><code>.one()</code></strong>—return exactly one record (<code>Optional[Model]</code>), raise on multiple","<strong><code>.count()</code></strong>—return count of matching records (<code>int</code>), generates <code>SELECT COUNT(*)</code>","<strong><code>.exists()</code></strong>—return whether records exist (<code>bool</code>), short-circuit optimized","<strong><code>.to_sql()</code></strong>—return the SQL string and parameter tuple <code>(str, tuple)</code> without executing"]},
{tag:"h2",html:"Intermediate Methods"},
{tag:"p",html:"Intermediate methods build the query chain and can be freely combined:"},
{tag:"ul",items:["<strong><code>.where(predicate)</code></strong>—add filter conditions, multiple calls form AND","<strong><code>.select(*columns)</code></strong>—specify return columns, defaults to <code>SELECT *</code>","<strong><code>.order_by(*clauses)</code></strong>—specify sort order, supports <code>(column, 'ASC')</code> / <code>(column, 'DESC')</code> tuples","<strong><code>.limit(n)</code></strong>—limit returned rows","<strong><code>.offset(n)</code></strong>—pagination offset","<strong><code>.join(model, on=condition, alias=name)</code></strong>—table joins","<strong><code>.group_by(*columns)</code></strong>—group aggregation","<strong><code>.having(condition)</code></strong>—group filtering"]},
{tag:"h2",html:"Expression-Driven Queries via <code>.c</code>"},
{tag:"p",html:"Conditions are constructed through <code>User.c.field</code> expressions, not keyword arguments. This is rhosocial-activerecord's key design choice—leveraging Python's operator overloading:"},
{tag:"ul",items:["<strong>.where(User.c.age >= 18)</strong>—Python operator overloads construct query conditions","<strong>.where(User.c.name.like(\"%Alice%\"))</strong>—string matching methods","<strong>.where(User.c.id.in_([1, 2, 3]))</strong>—set membership","<strong>.where(User.c.deleted_at.is_null())</strong>—null checking"]},
{tag:"p",html:"Each <code>User.c.field</code> produces a <code>Column</code> expression node supporting all Python comparison operators and expression methods. Expression nodes can call <code>.to_sql()</code> to inspect the generated SQL at any time."},
{tag:"h2",html:"Composability"},
{tag:"p",html:"ActiveQuery chaining supports free condition composition. Build different query variants based on conditional branching:"},
{tag:"code",lang:"python",text:"def search_users(name=None, min_age=None, active_only=False):\n    q = User.query()\n    if name:\n        q = q.where(User.c.name.like(f\"%{name}%\"))\n    if min_age:\n        q = q.where(User.c.age >= min_age)\n    if active_only:\n        q = q.where(User.c.status == \"active\")\n    return q.order_by((User.c.name, \"ASC\")).all()"},
{tag:"p",html:"Each <code>.where()</code> returns a new ActiveQuery instance—conditions are accumulated, not mutated. This lets query-building functions branch safely without modifying the original query."},
{tag:"h2",html:"Pydantic Integration"},
{tag:"p",html:"<code>save()</code> triggers a full Pydantic field validation before database operations. Even if field values are set bypassing type annotations, data issues are caught before persistence."},
{tag:"p",html:"<code>save(validate=False)</code> skips validation for performance-sensitive or pre-validated scenarios."},
{tag:"h2",html:"Summary"},
{tag:"p",html:"rhosocial-activerecord's fluent API follows three principles:"},
{tag:"ul",items:["<strong>Readability first</strong>—<code>user.save()</code> / <code>user.delete()</code> read as natural language, not symbol sequences","<strong>Lazy evaluation</strong>—query construction and execution are separated; <code>.to_sql()</code> provides transparency","<strong>Composability</strong>—conditions are freely composable building blocks; each <code>.where()</code> returns a new instance"]},
{tag:"hr"},
{tag:"next",html:"Next: Relation Mapping—belongs_to / has_many declarative definitions and loading strategies."}]})})();