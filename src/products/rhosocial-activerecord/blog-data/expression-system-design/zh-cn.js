(function(){var w=window.I18N;var d=w&&w['zh-cn'];if(!d)return;Object.assign(d,{hero:{ back: "返回 Blog", category:"查询 · 架构",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"}, title: "Expression 系统的设计哲学", sub: "为什么选择表达式树而非字符串拼接来生成 SQL。" },body:[ { tag: "p", html: "<strong>2026-05-19</strong> · by rhosocial team" }, { tag: "lead", html: "SQL 生成是 ORM 最核心的技术挑战之一。AR 选择了<strong>表达式树（Expression Tree）</strong>而非字符串拼接来生成 SQL。这篇文章将解释为什么表达式树是更好的选择，以及 AR 表达式系统的设计细节。" }, { tag: "h2", html: "字符串拼接的问题" }, { tag: "p", html: "许多 ORM 和查询构建器使用字符串拼接来生成 SQL。例如：" }, { tag: "code", text: "# 字符串拼接方式\nquery = \"SELECT * FROM users\"\nif name:\n query += f\" WHERE name = '{name}'\"\nquery += \" ORDER BY id\"" }, { tag: "p", html: "这种方式有几个严重问题：" }, { tag: "ul", items: [ "<strong>SQL 注入风险</strong>：字符串拼接是 SQL 注入的主要来源。即使使用参数化查询，拼接逻辑也容易出错", "<strong>类型不安全</strong>：无法在编译期检查字段名、操作符的正确性", "<strong>方言差异难处理</strong>：不同数据库的 SQL 语法不同（分页、函数名、类型字面量），字符串拼接难以抽象", "<strong>缺乏组合性</strong>：复杂的 WHERE 条件（嵌套 AND/OR、子查询）用字符串表达非常脆弱", "<strong>无优化空间</strong>：字符串一旦拼接完成，就无法再分析或优化查询结构" ]}, { tag: "h2", html: "表达式树方案" }, { tag: "p", html: "AR 的 Expression 系统将 SQL 查询的每个部分抽象为<strong>表达式节点</strong>。每个节点代表 SQL AST（抽象语法树）中的一个元素——字段引用、比较操作、逻辑组合、函数调用等。整个查询构建过程就是构建和组合这些节点的过程。" }, { tag: "code", text: "# 表达式的构建过程\nfrom rhosocial.activerecord import Expression as Expr\n\n# 构建比较表达式\ncondition = Expr.field(\"age\") >= 18\n# 内部表示为: Comparison(Field(\"age\"), Op.GTE, Literal(18))\n\n# 组合条件\ncombined = (Expr.field(\"age\") >= 18) & (Expr.field(\"status\") == \"active\")\n# 内部表示为: And(Comparison(...), Comparison(...))\n\n# 传递给查询构建器\nquery = User.query().where(combined)" }, { tag: "p", html: "使用表达式树的好处包括：" }, { tag: "ul", items: [ "<strong>类型安全</strong>：字段名在 IDE 中可以获得补全，操作符是类型化的枚举而非字符串", "<strong>SQL 注入免疫</strong>：所有用户输入都通过参数化占位符传递，值永远不会拼接到 SQL 字符串中", "<strong>方言抽象</strong>：表达式树在渲染为 SQL 时由 Dialect 系统处理，不同后端生成不同的 SQL 但在表达式层面保持一致", "<strong>可组合</strong>：表达式可以嵌套、组合、复用——就像构建 AST 一样自然", "<strong>可优化</strong>：在执行前可以遍历表达式树进行优化——去掉恒真/恒假条件、合并重复条件、下推过滤条件" ]}, { tag: "h2", html: "表达式类型" }, { tag: "p", html: "AR 的 Expression 系统支持以下表达式类型：" }, { tag: "ul", items: [ "<strong>字面量（Literal）</strong>：整数、字符串、浮点数、布尔值、NULL 等", "<strong>字段引用（Field）</strong>：指向模型中的某个字段，可带表名前缀", "<strong>比较运算（Comparison）</strong>：=、!=、&gt;、&gt;=、&lt;、&lt;=、IN、NOT IN、LIKE、IS NULL、IS NOT NULL 等", "<strong>逻辑组合（Logical）</strong>：AND、OR、NOT——支持无限嵌套", "<strong>算术运算（Arithmetic）</strong>：+、-、*、/、%——用于字段运算和计算列", "<strong>函数调用（Function）</strong>：COUNT、SUM、AVG、MAX、MIN、COALESCE、UPPER、LOWER、LENGTH 等聚合和标量函数", "<strong>子查询（Subquery）</strong>：嵌套的 SELECT 查询，可作为表达式或条件使用", "<strong>参数化占位符（Parameter）</strong>：所有用户输入值都通过此节点传递，值永不进入 SQL 字符串" ]}, { tag: "h2", html: "通用表达式的方言无关设计" }, { tag: "p", html: "AR 的表达式系统是<strong>方言无关</strong>的。核心库定义了通用表达式类，它们在任何数据库后端上表现一致。例如 <code>Expr.field(\"age\") >= 18</code> 在所有后端的含义相同——生成 <code>age >= ?</code>（含参数化占位符）。" }, { tag: "p", html: "但某些表达式在不同的数据库中有不同的 SQL 语法。例如 LIMIT/OFFSET 分页：" }, { tag: "ul", items: [ "SQLite/MySQL/PostgreSQL：<code>LIMIT ? OFFSET ?</code>", "SQL Server：<code>OFFSET ? ROWS FETCH NEXT ? ROWS ONLY</code>", "Oracle：<code>OFFSET ? ROWS FETCH NEXT ? ROWS ONLY</code>" ]}, { tag: "p", html: "这些差异不在 Expression 层处理，而是由 Dialect 系统在 SQL 渲染阶段解决。表达式树始终保持一致，Dialect 负责把它翻译为目标数据库的 SQL 方言。" }, { tag: "h2", html: "Dummy 演示后端" }, { tag: "p", html: "AR 提供了一个 <strong>dummy 演示后端</strong>，用于验证 Expression 系统的正确性。这个后端不连接任何数据库，只做两件事：" }, { tag: "ul", items: [ "接收表达式树并将其渲染为 SQL 字符串（方便调试和验证）", "验证表达式树的合法性和完整性" ]}, { tag: "p", html: "Dummy 后端是 Expression 系统的\"测试金标准\"——如果一个表达式在 dummy 后端上的渲染结果正确，那么它在所有数据库后端上的语义也应该一致。" }, { tag: "h2", html: "总结" }, { tag: "p", html: "AR 的 Expression 系统通过表达式树实现了类型安全、方言无关、可组合、可优化的 SQL 生成。它与 Dialect 系统配合，让复杂的 SQL 语义在不同数据库之间保持一致。" }, { tag: "hr" }, { tag: "next", html: "下一篇：Dialect 系统——通用表达式与方言扩展，各后端如何根据自身特性扩展方言。" } ]})})();