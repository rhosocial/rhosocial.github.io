(function(){var w=window.I18N;var d=w&&w["en-us"];if(!d)return;Object.assign(d,{hero:{category:"Architecture · Backend",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team"},body:[{ tag: "lead", html: "At first glance, the idea of separating core ActiveRecord from database backends seems obvious: ActiveRecord talks to databases, so naturally it needs database drivers. But the real motivation runs deeper. It's not about enabling database access—it's about ensuring behavior consistency across different databases while respecting each backend's unique semantics. This article explains why a backend abstraction layer is essential, not optional." },{ tag: "h2", html: "The Core Challenge: Same Pattern, Different Databases" },{ tag: "p", html: "All relational databases roughly follow the relational model, but each one has its own personality. Consider these everyday scenarios:" },{ tag: "ul", items: ["<strong>Auto-increment</strong>: MySQL uses <code>AUTO_INCREMENT</code>, PostgreSQL uses <code>SERIAL</code>/<code>IDENTITY</code>, SQLite uses <code>AUTOINCREMENT</code>, Oracle uses sequences","<strong>Pagination</strong>: MySQL/MariaDB/SQLite use <code>LIMIT ... OFFSET</code>, SQL Server uses <code>OFFSET ... FETCH NEXT</code> or <code>TOP</code>, Oracle 11g uses <code>ROWNUM</code> while 12c+ uses <code>OFFSET ... FETCH</code>","<strong>JSON support</strong>: PostgreSQL has native JSON operators (<code>-></code>, <code>->></code>), MySQL has <code>JSON_EXTRACT()</code>, SQLite added JSON functions in 3.38","<strong>Transaction isolation</strong>: <code>READ COMMITTED</code> is the default in PostgreSQL but <code>REPEATABLE READ</code> in MySQL InnoDB, and <code>SERIALIZABLE</code> semantics differ subtly between databases","<strong>Returning clause</strong>: PostgreSQL supports <code>INSERT ... RETURNING</code> natively; MySQL 8.0.21+ needs workarounds; SQL Server uses <code>OUTPUT</code>"]},{ tag: "p", html: "If an ORM simply generates SQL strings with parameter substitution, each of these differences becomes a special case scattered across the codebase. The result is fragile, untestable, and database-specific logic leaking into every query operation." },{ tag: "h2", html: "Why Not Just Use SQL in ActiveRecord?" },{ tag: "p", html: "Some might argue: if each database is different, why not let ActiveRecord remain simple and push all complexity into the SQL? There are three problems with this approach:" },{ tag: "ul", items: ["<strong>SQL injection risk</strong>: This is the most fundamental reason. Direct SQL concatenation is the #1 web security threat. ActiveRecord must eliminate injection risk entirely through parameterized queries, which requires backends to follow PEP 249 (Python Database API Specification v2.0)—all parameters passed through <code>execute(sql, params)</code>, never interpolated into SQL strings. This principle runs through the entire architecture and is non-negotiable","Application code becomes littered with database-specific SQL, negating the portability promise of an ORM","The ActiveRecord pattern—where model objects handle their own persistence—becomes impossible to implement generically when every CRUD operation needs database-specific SQL generation"]},{ tag: "callout", html: "<strong>Security principle:</strong> ActiveRecord does not accept raw SQL string concatenation. All queries must be built through the expression system, and all parameters must be passed through DB API 2.0's parameterized mechanism. This is not a limitation—it's a guarantee, eliminating SQL injection at the architectural level." },{ tag: "p", html: "This is the mission of the backend abstraction layer: not to eliminate differences—that's impossible—but to <strong>abstract them behind a consistent interface</strong>, encapsulating the complexity within the backend. ActiveRecord itself never needs to know which database it's talking to—that's the backend layer's responsibility, not the upper layer's concern." },{ tag: "h2", html: "Backend as a Decoupled Module, Not an ActiveRecord Dependency" },{ tag: "p", html: "A critical design decision in rhosocial ActiveRecord is that the backend is <strong>not a part of ActiveRecord</strong>. Instead, the backend is an independent module that can work on its own. ActiveRecord is just one consumer of the backend—not the other way around." },{ tag: "p", html: "This means:" },{ tag: "ul", items: ["The backend manages connections, executes SQL, and handles transactions without any knowledge of ActiveRecord models","ActiveRecord calls the backend through a clean interface, treating it as a persistence engine","Third-party tools or scripts can use the backend directly without loading ActiveRecord models at all"]},{ tag: "p", html: "This decoupling ensures that backend evolution does not require ActiveRecord changes, and vice versa. A backend can add connection pooling optimizations, implement new transaction isolation levels, or fix driver-specific bugs—all without modifying a single line in the core ActiveRecord library." },{ tag: "p", html: "More importantly, <strong>the backend's goal is to fully cover database dialect semantics, not merely satisfy ActiveRecord's requirements</strong>. Every database has rich features and semantics—from PostgreSQL's JSONB operators to MySQL's fulltext search, from SQLite's PRAGMA directives to Oracle's hierarchical queries. Backends should faithfully expose these capabilities rather than implementing only the minimal subset needed for ActiveRecord CRUD. ActiveRecord is a consumer of backends; backends should not be constrained by ActiveRecord's feature scope." },{ tag: "h2", html: "Beyond SQL Strings: A Common Expression System" },{ tag: "p", html: "Abstracting SQL generation is the obvious piece. What's less obvious—and far more powerful—is abstracting query semantics themselves through a <strong>common expression system</strong>." },{ tag: "p", html: "rhosocial ActiveRecord defines a set of universal expression nodes that represent query operations independent of any database dialect:" },{ tag: "ul", items: ["<strong>Comparison expressions</strong>: equals, not equals, greater than, less than, IN, BETWEEN, LIKE, IS NULL","<strong>Logical expressions</strong>: AND, OR, NOT combining arbitrary sub-expressions","<strong>Aggregate expressions</strong>: COUNT, SUM, AVG, MIN, MAX with optional DISTINCT and GROUP BY","<strong>Function expressions</strong>: date manipulation, string operations, mathematical functions","<strong>Subquery expressions</strong>: correlated and uncorrelated subqueries in SELECT, FROM, WHERE, and HAVING clauses"]},{ tag: "p", html: "The true killer features of this expression system are two core mechanisms: <strong>BaseExpression</strong> and <strong>ToSQLProtocol</strong>." },
{ tag: "ul", items: [
  "<strong>BaseExpression</strong>—the unified base class for all expression nodes. It defines the fundamental contract of the expression tree: child node management (<code>children</code> property), deep copying, visitor traversal, and more. Every comparison, logical, aggregate, or function node ultimately inherits from BaseExpression. This design—treating expression trees as pure data structures—is the cornerstone of the entire backend abstraction layer.",
  "<strong>ToSQLProtocol</strong>—the protocol that compiles any expression node into SQL. Each backend dialect implements this protocol to translate the abstract expression tree into database-specific SQL strings. The protocol uses a composition pattern: each node knows how to translate itself into its corresponding SQL fragment, and parent nodes recursively compose these fragments into complete statements. This ensures the translation logic is composable and independently testable."
]},
{ tag: "p", html: "Each backend then translates this expression tree into database-specific SQL. The expression system itself knows nothing about MySQL, PostgreSQL, or Oracle—it's a pure data structure. Each backend's dialect implementation contains all the translation logic." },{ tag: "h3", html: "The Dummy Backend: Expression Validation Without a Database" },{ tag: "p", html: "A powerful application of the common expression system is the <strong>dummy backend</strong>—a backend implementation that doesn't connect to any real database. It only builds and validates expression trees without executing any SQL. This is useful in several scenarios:" },{ tag: "ul", items: ["<strong>Testing and CI</strong>: Validate query construction logic in CI pipelines without database environments","<strong>Query preview</strong>: Let developers preview generated SQL in the IDE or debugger without connecting to a database","<strong>Education and demos</strong>: Showcase rhosocial-activerecord's query API in documentation and tutorials where readers don't need to install any database","<strong>API contract validation</strong>: Catch query interface misuse at construction time rather than execution time"]},{ tag: "code", text: "# Dummy backend example: build queries without execution\nfrom rhosocial.activerecord.backend.impl.dummy import DummyBackend\n\nbackend = DummyBackend()\nquery = User.query().where(User.c.age > 18).order_by(User.c.name)\n\n# Get the expression tree (no database connection)\nexpr_tree = query.build_expression()\nprint(expr_tree)  # Inspect expression structure\n\n# Get Dialect-translated SQL (no execution)\nsql, params = backend.dialect.compile(expr_tree)\nprint(sql)     # SELECT ... FROM users WHERE age > ?\nprint(params)  # [18]" },{ tag: "p", html: "The existence of the dummy backend proves the pure-data nature of the expression system—expression trees are pure, inspectable, serializable data structures that don't depend on any I/O operations. This is also the foundation of the sync/async parity design: expression construction is pure computation; only the final SQL execution involves I/O." },{ tag: "h2", html: "Respecting Backend-Specific Semantics" },{ tag: "p", html: "A one-size-fits-all expression system would be too rigid. Different databases have genuinely different capabilities and semantics that developers should be able to leverage:" },{ tag: "ul", items: ["<strong>PostgreSQL-specific operators</strong>: <code>->></code> for JSON fields, <code>@></code> for array containment, <code>SIMILAR TO</code> for regex matching","<strong>MySQL-specific clauses</strong>: <code>ON DUPLICATE KEY UPDATE</code>, fulltext search with <code>MATCH ... AGAINST</code>, <code>GROUP_CONCAT</code>","<strong>SQLite extensions</strong>: <code>UPSERT</code> (since 3.24), <code>JSON</code> functions (since 3.38), <code>STRICT</code> tables","<strong>SQL Server features</strong>: <code>TOP</code> with ties, <code>OUTPUT</code> clause, <code>PIVOT</code>/<code>UNPIVOT</code>, <code>APPLY</code> operator","<strong>Oracle capabilities</strong>: <code>CONNECT BY</code> for hierarchical queries, <code>MERGE</code> for upsert, <code>FLASHBACK</code> queries"]},{ tag: "p", html: "Rather than hiding these differences, the expression system provides <strong>extension points</strong> where backends can register custom expression nodes. The common expression tree serves as a safety net—ensuring portable queries work everywhere—while the extension mechanism allows each backend to expose its unique powers." },{ tag: "h2", html: "Backend Interface Architecture" },{ tag: "p", html: "The backend interface is designed around four core components, each independently replaceable:" },{ tag: "ul", items: ["<strong>Connection Manager</strong>: handles connection lifecycle, pooling strategies, and driver-specific configuration. Does not depend on ActiveRecord models.","<strong>Dialect</strong>: translates expression trees into SQL strings and handles data type mapping. This is where database-specific syntax lives.","<strong>Expression Implementation</strong>: extends the common expression system with database-specific nodes and optimizations.","<strong>Migration Engine</strong>: manages schema versioning, DDL generation, and forward/rollback operations."]},{ tag: "p", html: "Each component is a plain Python class that implements a documented protocol. ActiveRecord calls these components through the backend interface, never directly." },{ tag: "h2", html: "ActiveQuery and Backend Collaboration: Query Pipeline" },{ tag: "p", html: "From ActiveRecord models to the underlying database, a single query traverses multiple abstraction layers. ActiveQuery and its variants sit between ActiveRecord and the Backend, translating high-level query operations into expression trees, which the Backend then compiles into SQL and executes." },{ tag: "raw", html: "<div class='diagram-pipeline'><div class='pipe-node'>App</div><div class='pipe-arrow'>→</div><div class='pipe-node'>ActiveRecord</div><div class='pipe-arrow'>→</div><div class='pipe-node highlight'>ActiveQuery</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Expression<br>Tree</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Dialect</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Backend</div><div class='pipe-arrow'>→</div><div class='pipe-node db'>DB</div><div class='pipe-return'>←─ <span style='color:var(--text-primary)'>Result</span> (Rows → Models) ─→</div></div>" },{ tag: "p", html: "The highlighted part is key: <strong>ActiveQuery</strong> does not generate SQL directly—it builds an <strong>Expression Tree</strong>. This tree is a pure description of the query intent—SELECT columns, WHERE conditions, ordering, pagination—completely independent of any SQL dialect. The Backend's <strong>Dialect</strong> then translates this tree into database-specific SQL." },{ tag: "p", html: "This means ActiveRecord never needs to know which database it's talking to, and the Backend can freely optimize SQL generation. All three query types—StandardQuery, CTEQuery, SetOperationQuery—follow this same collaboration pattern." },{ tag: "h2", html: "Three Query Types on the Expression System" },{ tag: "p", html: "rhosocial ActiveRecord defines three core query types. They share the same Expression system, but each builds and uses expression trees differently:" },{ tag: "raw", html: "<div class='diagram-tree'><div style='margin-bottom:var(--space-3);font-family:var(--font-display);font-size:14px;font-weight:700;color:var(--text-primary)'>Query Type Hierarchy <span style='font-weight:400;font-size:12px;color:var(--text-muted);font-family:var(--font-mono)'>// All three share the Expression system</span></div><div class='tree-node root'>ActiveQuery (Base)</div><div class='tree-children'><div class='tree-node'>StandardQuery <span class='tree-ann'>→ WHERE, ORDER BY, LIMIT</span></div><div class='tree-node' style='border-color:var(--accent);background:var(--accent-soft);color:var(--accent)'>CTEQuery <span class='tree-ann'>→ WITH ... AS (...)</span></div><div class='tree-node' style='border-color:var(--accent-secondary);background:var(--accent-secondary-soft);color:var(--accent-secondary)'>SetOperationQuery <span class='tree-ann'>→ UNION / INTERSECT / EXCEPT</span></div></div></div>" },{ tag: "ul", items: ["<strong>StandardQuery</strong>: The most common type. Calls to <code>.where()</code>, <code>.order_by()</code>, <code>.limit()</code> are translated into expression nodes, ultimately producing a standard <code>SELECT</code> statement.","<strong>CTEQuery</strong>: Extends the expression system via the <code>WITH</code> clause. CTE definitions and the main query each build their own expression trees, which CTEQuery combines into one complete tree.","<strong>SetOperationQuery</strong>: Merges multiple query expression trees into one. The root identifies the set operation type (UNION / INTERSECT / EXCEPT), and the subtrees represent each participating query."] },{ tag: "p", html: "The collaboration pattern is unified: each query type builds a <strong>complete expression tree</strong>, the root determines the query type, and subtrees carry conditions and parameters. The Backend's Dialect traverses this tree to generate the final SQL, completely transparent to the upper layers." },{ tag: "h2", html: "Sync/Async Parity: Same Architecture, Two Execution Paths" },{ tag: "p", html: "rhosocial ActiveRecord follows the <strong>Sync/Async Parity</strong> principle: sync and async APIs have identical method names and semantics. The only differences are the Async prefix on class names and the <code>await</code> keyword on calls." },{ tag: "raw", html: "<div class='diagram-compare'><div class='compare-col'><div class='compare-header sync'>Sync</div><div class='compare-item'><span class='dim'>class</span> <span class='hl'>User</span>(ActiveRecord):<br>&nbsp;&nbsp;pass<br><br><span class='dim'># Sync query</span><br>users = <span class='hl'>User</span>.query()\<br>&nbsp;&nbsp;.where(<span class='hl'>User</span>.c.age &gt; 18)\<br>&nbsp;&nbsp;.order_by(<span class='hl'>User</span>.c.name)\<br>&nbsp;&nbsp;.all()</div><div class='compare-item'><span class='dim'>SyncBackend.execute(sql)</span></div><div class='compare-caption'>← Shared Expression Tree →</div></div><div class='compare-col'><div class='compare-header async'>Async</div><div class='compare-item'><span class='dim'>class</span> <span class='hl2'>AsyncUser</span>(AsyncActiveRecord):<br>&nbsp;&nbsp;pass<br><br><span class='dim'># Async query</span><br>users = <span class='kw'>await</span> <span class='hl2'>AsyncUser</span>.query()\<br>&nbsp;&nbsp;.where(<span class='hl2'>AsyncUser</span>.c.age &gt; 18)\<br>&nbsp;&nbsp;.order_by(<span class='hl2'>AsyncUser</span>.c.name)\<br>&nbsp;&nbsp;.all()</div><div class='compare-item'><span class='dim'>await AsyncBackend.execute(sql)</span></div><div class='compare-caption'>← Shared Expression Tree →</div></div></div>" },{ tag: "p", html: "The key insight: <strong>expression tree construction is I/O-free</strong>. Whether sync or async, <code>.where()</code>, <code>.order_by()</code> methods are pure in-memory operations—building trees, merging conditions, appending parameters. No <code>await</code> needed." },{ tag: "p", html: "The <strong>only difference</strong> between sync and async is at SQL execution time: sync backends block on the database driver, async backends <code>await</code> the async driver. Both share the same model definitions, query builder, and expression system—the divergence happens only in the final leg of the journey." },{ tag: "blockquote", html: "<strong>Design metaphor</strong>: Think of the expression system as a <strong>recipe</strong>—listing ingredients (columns), steps (conditions), and heat (ordering). Writing a recipe doesn't require turning on the stove (I/O). Only when you actually cook (execute SQL) does it matter. Sync chefs and async chefs use the same recipe." },{ tag: "h2", html: "Built-in SQLite: The Reference Backend" },{ tag: "p", html: "SQLite is included in the core library—not because it's special, but because it serves as the <strong>reference implementation</strong> of the backend interface. New backend developers can study the SQLite dialect to understand the contract they need to fulfill." },{ tag: "p", html: "SQLite also enables the zero-configuration startup experience: <code>pip install rhosocial-activerecord</code> and immediately define models, create tables, and run CRUD. No database server setup required." },{ tag: "h2", html: "Contract Testing: Consistency Across Backends" },{ tag: "p", html: "Abstract interfaces alone are not enough. Behavior consistency requires <strong>contract testing</strong>. The <code>python-activerecord-testsuite</code> project defines a set of test contracts that every backend must pass:" },{ tag: "ul", items: ["CRUD operations behave identically across all backends","Query builder produces semantically equivalent results","Transaction semantics (commit, rollback, savepoints) are consistent","Migration operations produce equivalent schema states","Data type mappings cover the full type spectrum"]},{ tag: "p", html: "A backend that passes all contract tests guarantees that application code written against it will work identically on any other compliant backend—without SQL-level differences leaking into the application layer." },{ tag: "h2", html: "Summary" },{ tag: "p", html: "The core-backend separation in rhosocial ActiveRecord is driven by a single, pragmatic motivation: <strong>relational databases share the relational model but differ in the details</strong>. A backend abstraction layer is the proven way to manage this tension." },{ tag: "ul", items: ["The backend is a decoupled module that works independently—ActiveRecord is its user, not its owner","A common expression system provides portable query semantics","Extension points allow backends to expose database-specific capabilities","Contract testing guarantees behavioral consistency across all backends","The built-in SQLite backend serves as both the zero-config default and a reference implementation"]},{ tag: "hr" },{ tag: "next", html: "Next: Sync/Async API symmetry—unified naming conventions and the design philosophy behind them." }]})})();