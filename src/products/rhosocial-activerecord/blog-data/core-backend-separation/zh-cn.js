(function(){var w=window.I18N;var d=w&&w["zh-cn"];if(!d)return;Object.assign(d,{hero:{category:"架构 · 后端",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"},author:"by rhosocial 团队"},body:[{ tag: "lead", html: "乍看起来，将核心 ActiveRecord 与数据库后端分离的想法似乎理所当然：ActiveRecord 需要访问数据库，所以它自然需要数据库驱动。但真正的动机比这更深刻。这不仅仅是为了让数据库访问成为可能，而是为了在确保跨数据库行为一致性的同时，尊重每个后端独特的语义。这篇文章将解释为什么后端抽象层是必不可少的，而不是可选的。" },{ tag: "h2", html: "核心挑战：同一模式，不同数据库" },{ tag: "p", html: "所有关系型数据库都大致遵循关系模型，但每个数据库都有自己的个性。考虑这些日常场景：" },{ tag: "ul", items: ["<strong>自增字段</strong>：MySQL 使用 <code>AUTO_INCREMENT</code>，PostgreSQL 用 <code>SERIAL</code>/<code>IDENTITY</code>，SQLite 用 <code>AUTOINCREMENT</code>，Oracle 用序列","<strong>分页</strong>：MySQL/MariaDB/SQLite 用 <code>LIMIT ... OFFSET</code>，SQL Server 用 <code>OFFSET ... FETCH NEXT</code> 或 <code>TOP</code>，Oracle 11g 用 <code>ROWNUM</code> 而 12c+ 用 <code>OFFSET ... FETCH</code>","<strong>JSON 支持</strong>：PostgreSQL 有原生 JSON 操作符（<code>-></code>、<code>->></code>），MySQL 有 <code>JSON_EXTRACT()</code>，SQLite 在 3.38 后增加了 JSON 函数","<strong>事务隔离</strong>：PostgreSQL 默认是 <code>READ COMMITTED</code>，MySQL InnoDB 是 <code>REPEATABLE READ</code>，<code>SERIALIZABLE</code> 的语义在不同数据库之间有微妙差异","<strong>RETURNING 子句</strong>：PostgreSQL 原生支持 <code>INSERT ... RETURNING</code>；MySQL 8.0.21+ 需要变通方案；SQL Server 使用 <code>OUTPUT</code>"]},{ tag: "p", html: "如果一个 ORM 只是简单地做 SQL 字符串拼接和参数替换，那么每个这样的差异都会变成散落在代码库中的特例分支。结果是脆弱的、难以测试的、数据库特定的逻辑泄漏到每一个查询操作中。" },{ tag: "h2", html: "为什么不让 ActiveRecord 直接用 SQL？" },{ tag: "p", html: "有人可能会说：既然每个数据库都不一样，为什么不让 ActiveRecord 保持简单，把所有复杂性推给 SQL？这个做法有三个问题：" },{ tag: "ul", items: ["<strong>SQL 注入风险</strong>：这是最根本的原因。直接拼接 SQL 是 Web 安全的头号威胁。ActiveRecord 必须通过参数化查询来彻底消除注入风险，而参数化查询要求后端遵循 PEP 249（Python Database API Specification v2.0）——所有参数通过 <code>execute(sql, params)</code> 传递，绝不拼入 SQL 字符串。这一原则贯穿整个架构，不可动摇","应用代码中会充斥着数据库特定的 SQL，让 ORM 的可移植性承诺化为泡影","ActiveRecord 模式——模型对象自己处理持久化——当每个 CRUD 操作都需要生成数据库特定的 SQL 时，实际上无法通用地实现"]},{ tag: "callout", html: "<strong>安全原则：</strong>ActiveRecord 不接受原始 SQL 字符串拼接。所有查询必须通过表达式系统构建，所有参数必须通过 DB API 2.0 的参数化机制传递。这不是限制，而是保障——从架构层面根除 SQL 注入的可能性。" },{ tag: "p", html: "解决方案不是消除差异——那是不可能的——而是<strong>在一致的接口背后抽象这些差异</strong>，让 ActiveRecord 本身永远不需要知道它在和哪个数据库对话。" },{ tag: "h2", html: "后端是解耦的独立模块，而非 ActiveRecord 的一部分" },{ tag: "p", html: "rhosocial ActiveRecord 的一个关键设计决策是：后端<strong>不是 ActiveRecord 的一部分</strong>。相反，后端是一个可以独立工作的模块。ActiveRecord 只是后端的一个消费者——而不是反过来。" },{ tag: "p", html: "这意味着：" },{ tag: "ul", items: ["后端管理连接、执行 SQL、处理事务，完全不需要知道 ActiveRecord 模型的存在","ActiveRecord 通过一个干净的接口调用后端，将其视为一个持久化引擎","第三方工具或脚本可以直接使用后端，而根本不需要加载 ActiveRecord 模型"]},{ tag: "p", html: "这种解耦确保了后端的演进不需要 ActiveRecord 的变更，反之亦然。后端可以增加连接池优化、实现新的事务隔离级别、修复特定驱动的 bug——所有这些都不需要修改核心 ActiveRecord 库中的一行代码。" },{ tag: "p", html: "更重要的是，<strong>后端的目标是全力覆盖数据库方言的语义，而非仅仅满足 ActiveRecord 的需求</strong>。每个数据库都有丰富的特性和语义——从 PostgreSQL 的 JSONB 操作符到 MySQL 的全文搜索，从 SQLite 的 PRAGMA 指令到 Oracle 的层次化查询——后端应该忠实地暴露这些能力，而不是只实现 ActiveRecord CRUD 所需的最小子集。ActiveRecord 是后端的消费者，后端不应被 ActiveRecord 的功能范围所限制。" },{ tag: "h2", html: "超越 SQL 字符串：通用表达式系统" },{ tag: "p", html: "抽象 SQL 生成是显而易见的部分。不那么明显——但更强大——的是通过<strong>通用表达式系统</strong>来抽象查询语义。" },{ tag: "p", html: "rhosocial ActiveRecord 定义了一组通用的表达式节点，表示独立于任何数据库方言的查询操作：" },{ tag: "ul", items: ["<strong>比较表达式</strong>：等于、不等于、大于、小于、IN、BETWEEN、LIKE、IS NULL","<strong>逻辑表达式</strong>：AND、OR、NOT 组合任意子表达式","<strong>聚合表达式</strong>：COUNT、SUM、AVG、MIN、MAX，支持可选的 DISTINCT 和 GROUP BY","<strong>函数表达式</strong>：日期处理、字符串操作、数学函数","<strong>子查询表达式</strong>：在 SELECT、FROM、WHERE、HAVING 子句中的相关和非相关子查询"]},{ tag: "p", html: "每个后端负责将这个表达式树翻译为数据库特定的 SQL。表达式系统本身对 MySQL、PostgreSQL 或 Oracle 一无所知——它只是一个纯数据结构。每个后端的方言实现包含了所有的翻译逻辑。" },{ tag: "h3", html: "Dummy 后端：无需数据库的表达式验证" },{ tag: "p", html: "通用表达式系统的一个强大应用是 <strong>dummy 后端</strong>——一个不连接任何真实数据库的后端实现。它只做表达式树的构建和校验，不执行任何 SQL。这在以下场景中非常有用：" },{ tag: "ul", items: ["<strong>测试和 CI</strong>：在没有数据库环境的 CI 管道中验证查询构建逻辑的正确性","<strong>查询预览</strong>：开发者在 IDE 或调试器中预览生成的 SQL，无需连接数据库","<strong>教学和演示</strong>：在文档和教程中展示 rhosocial-activerecord 的查询 API，读者无需安装任何数据库","<strong>API 契约验证</strong>：确认查询接口调用链的合法性，在构建阶段而非执行阶段发现错误"]},{ tag: "code", text: "# Dummy 后端示例：构建查询但不执行\nfrom rhosocial.activerecord.backend.impl.dummy import DummyBackend\n\nbackend = DummyBackend()\nquery = User.query().where(User.c.age > 18).order_by(User.c.name)\n\n# 获取生成的表达式树（不连接数据库）\nexpr_tree = query.build_expression()\nprint(expr_tree)  # 查看表达式结构\n\n# 获取 Dialect 翻译后的 SQL（不执行）\nsql, params = backend.dialect.compile(expr_tree)\nprint(sql)     # SELECT ... FROM users WHERE age > ?\nprint(params)  # [18]" },{ tag: "p", html: "Dummy 后端的存在证明了表达式系统的纯数据本质——表达式树是纯粹的、可检查的、可序列化的数据结构，它不依赖任何 I/O 操作。这也是同步/异步对等设计的基础：表达式构建是纯计算，只有最终的 SQL 执行才涉及 I/O。" },{ tag: "h2", html: "尊重后端特有的语义" },{ tag: "p", html: "一刀切的表达式系统会过于僵化。不同的数据库拥有真正不同的能力和语义，开发者应该能够加以利用：" },{ tag: "ul", items: ["<strong>PostgreSQL 特有的操作符</strong>：<code>->></code> 用于 JSON 字段、<code>@></code> 用于数组包含、<code>SIMILAR TO</code> 用于正则匹配","<strong>MySQL 特有的子句</strong>：<code>ON DUPLICATE KEY UPDATE</code>、使用 <code>MATCH ... AGAINST</code> 的全文搜索、<code>GROUP_CONCAT</code>","<strong>SQLite 扩展</strong>：<code>UPSERT</code>（3.24 起）、<code>JSON</code> 函数（3.38 起）、<code>STRICT</code> 表","<strong>SQL Server 特性</strong>：带 ties 的 <code>TOP</code>、<code>OUTPUT</code> 子句、<code>PIVOT</code>/<code>UNPIVOT</code>、<code>APPLY</code> 操作符","<strong>Oracle 能力</strong>：用于层次化查询的 <code>CONNECT BY</code>、用于 upsert 的 <code>MERGE</code>、<code>FLASHBACK</code> 查询"]},{ tag: "p", html: "表达式系统不是隐藏这些差异，而是提供了<strong>扩展点</strong>，让后端可以注册自定义的表达式节点。通用表达式树充当安全网——确保可移植查询在任何地方都能工作——同时扩展机制允许每个后端暴露其独特的能力。" },{ tag: "h2", html: "后端接口架构" },{ tag: "p", html: "后端接口围绕四个核心组件设计，每个组件都可以独立替换：" },{ tag: "ul", items: ["<strong>连接管理器（Connection Manager）</strong>：处理连接生命周期、连接池策略和驱动特定配置。不依赖于 ActiveRecord 模型。","<strong>方言（Dialect）</strong>：将表达式树翻译为 SQL 字符串，处理数据类型映射。数据库特定的语法就放在这里。","<strong>表达式实现（Expression Implementation）</strong>：用数据库特定的节点和优化来扩展通用表达式系统。","<strong>迁移引擎（Migration Engine）</strong>：管理 schema 版本控制、DDL 生成和前向/回滚操作。"]},{ tag: "p", html: "每个组件都是一个实现了文档化协议的普通 Python 类。ActiveRecord 通过后端接口调用这些组件，从不直接访问。" },{ tag: "h2", html: "ActiveQuery 与 Backend 的协作：查询执行链路" },{ tag: "p", html: "从 ActiveRecord 模型到底层数据库，一次查询的执行需要跨越多个抽象层。ActiveQuery 及其变体位于 ActiveRecord 和 Backend 之间，负责将高级查询操作转换为表达式树，再由 Backend 编译为 SQL 并执行。" },{ tag: "raw", html: "<div class='diagram-pipeline'><div class='pipe-node'>App</div><div class='pipe-arrow'>→</div><div class='pipe-node'>ActiveRecord</div><div class='pipe-arrow'>→</div><div class='pipe-node highlight'>ActiveQuery</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Expression<br>Tree</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Dialect</div><div class='pipe-arrow'>→</div><div class='pipe-node'>Backend</div><div class='pipe-arrow'>→</div><div class='pipe-node db'>DB</div><div class='pipe-return'>←─ <span style='color:var(--text-primary)'><span data-i18n-value='pipeline.result'>Result</span></span> (Rows → Models) ─→</div></div>" },{ tag: "p", html: "图中高亮的部分是关键：<strong>ActiveQuery</strong> 并不直接生成 SQL，而是构建<strong>表达式树</strong>（Expression Tree）。这棵树是对查询意图的纯描述——包含 SELECT 列、WHERE 条件、排序和分页——但独立于具体的 SQL 方言。后端拿到这棵树后，由它的 <strong>Dialect</strong> 组件翻译为数据库特定的 SQL。" },{ tag: "p", html: "这意味着 ActiveRecord 层永远不需要知道它在和哪个数据库对话，后端也可以自由地优化 SQL 生成。三种查询类型——StandardQuery、CTEQuery、SetOperationQuery——都遵循这一协作模式。" },{ tag: "h2", html: "三种查询类型的表达式协作" },{ tag: "p", html: "rhosocial ActiveRecord 定义了三种核心查询类型。它们共用同一个 Expression 系统，但各自以不同的方式构建和使用表达式树：" },{ tag: "raw", html: "<div class='diagram-tree'><div style='margin-bottom:var(--space-3);font-family:var(--font-display);font-size:14px;font-weight:700;color:var(--text-primary)'>Query Type Hierarchy <span style='font-weight:400;font-size:12px;color:var(--text-muted);font-family:var(--font-mono)'>// 三类查询共享 Expression 系统</span></div><div class='tree-node root'>ActiveQuery (Base)</div><div class='tree-children'><div class='tree-node'>StandardQuery <span class='tree-ann'>→ WHERE, ORDER BY, LIMIT</span></div><div class='tree-node' style='border-color:var(--accent);background:var(--accent-soft);color:var(--accent)'>CTEQuery <span class='tree-ann'>→ WITH ... AS (...)</span></div><div class='tree-node' style='border-color:var(--accent-secondary);background:var(--accent-secondary-soft);color:var(--accent-secondary)'>SetOperationQuery <span class='tree-ann'>→ UNION / INTERSECT / EXCEPT</span></div></div></div>" },{ tag: "ul", items: ["<strong>StandardQuery</strong>：最常用的查询类型。<code>.where()</code>、<code>.order_by()</code>、<code>.limit()</code> 等调用被转换为表达式节点，最终生成标准的 <code>SELECT</code> 语句。","<strong>CTEQuery</strong>：通过 <code>WITH</code> 子句扩展表达式系统。CTE 定义和主体查询各自独立构建表达式树，再由 CTEQuery 组合为一棵完整的树。","<strong>SetOperationQuery</strong>：将多个查询的表达式树合并，树的根节点标识集合操作类型（UNION / INTERSECT / EXCEPT），子树是各参与查询的表达式树。"] },{ tag: "p", html: "三种查询类型在表达式树层面的协作模式是统一的：每个查询都在构造一棵<strong>完整的表达式树</strong>，根节点决定查询类型，子树携带具体的条件和参数。后端的 Dialect 遍历这棵树并生成最终 SQL，对上层完全透明。" },{ tag: "h2", html: "同步与异步对等：同一架构，两种执行路径" },{ tag: "p", html: "rhosocial ActiveRecord 遵循<strong>同步/异步对等（Sync/Async Parity）</strong>原则：同步和异步 API 拥有完全相同的方法名和语义，唯一的区别是异步类加 Async 前缀，调用时使用 <code>await</code>。" },{ tag: "raw", html: "<div class='diagram-compare'><div class='compare-col'><div class='compare-header sync'>同步 Sync</div><div class='compare-item'><span class='dim'>class</span> <span class='hl'>User</span>(ActiveRecord):<br>&nbsp;&nbsp;pass<br><br><span class='dim'># 同步查询</span><br>users = <span class='hl'>User</span>.query()\<br>&nbsp;&nbsp;.where(<span class='hl'>User</span>.c.age &gt; 18)\<br>&nbsp;&nbsp;.order_by(<span class='hl'>User</span>.c.name)\<br>&nbsp;&nbsp;.all()</div><div class='compare-item'><span class='dim'>SyncBackend.execute(sql)</span></div><div class='compare-caption'>← 共享 Expression Tree →</div></div><div class='compare-col'><div class='compare-header async'>异步 Async</div><div class='compare-item'><span class='dim'>class</span> <span class='hl2'>AsyncUser</span>(AsyncActiveRecord):<br>&nbsp;&nbsp;pass<br><br><span class='dim'># 异步查询</span><br>users = <span class='kw'>await</span> <span class='hl2'>AsyncUser</span>.query()\<br>&nbsp;&nbsp;.where(<span class='hl2'>AsyncUser</span>.c.age &gt; 18)\<br>&nbsp;&nbsp;.order_by(<span class='hl2'>AsyncUser</span>.c.name)\<br>&nbsp;&nbsp;.all()</div><div class='compare-item'><span class='dim'>await AsyncBackend.execute(sql)</span></div><div class='compare-caption'>← 共享 Expression Tree →</div></div></div>" },{ tag: "p", html: "关键在于：<strong>表达式树的构建不涉及 I/O</strong>。无论是同步还是异步，<code>.where()</code>、<code>.order_by()</code> 等方法仅在内存中操作表达式节点——构建树、合并条件、追加参数。这些操作是纯计算的，不需要 <code>await</code>。" },{ tag: "p", html: "同步和异步的<strong>唯一区别</strong>在执行 SQL 的那一刻：同步后端阻塞调用数据库驱动，异步后端用 <code>await</code> 调用异步驱动。两者共享同一套模型定义、查询构建器和表达式系统——区别仅在最后一段旅程。" },{ tag: "blockquote", html: "<strong>设计隐喻</strong>：可以把表达式系统想象成一张<strong>食谱</strong>——列出食材（列）、步骤（条件）、火候（排序）。写食谱不需要开火（I/O），只有真正开始烹饪（执行 SQL）时才需要。同步厨师和异步厨师用的是同一张食谱。" },{ tag: "h2", html: "内置 SQLite：参考后端" },{ tag: "p", html: "SQLite 内置于核心库中——不是因为它特殊，而是因为它作为后端接口的<strong>参考实现</strong>。新的后端开发者可以研究 SQLite 方言来理解他们需要满足的契约。" },{ tag: "p", html: "SQLite 同时也实现了零配置启动体验：<code>pip install rhosocial-activerecord</code> 后立即可以定义模型、创建表、执行 CRUD，无需任何数据库服务器设置。" },{ tag: "h2", html: "契约测试：跨后端的一致性" },{ tag: "p", html: "仅有抽象接口是不够的。行为一致性需要<strong>契约测试</strong>。<code>python-activerecord-testsuite</code> 项目定义了一套每个后端必须通过的测试契约：" },{ tag: "ul", items: ["CRUD 操作在所有后端上行为一致","查询构建器产生语义等价的结果","事务语义（提交、回滚、保存点）一致","迁移操作产生等价的 schema 状态","数据类型映射覆盖完整的类型谱系"]},{ tag: "p", html: "一个通过了所有契约测试的后端，保证了针对它编写的应用代码在任何其他合规后端上都能产生相同的行为——而不会让 SQL 层面的差异泄漏到应用层。" },{ tag: "h2", html: "总结" },{ tag: "p", html: "rhosocial ActiveRecord 的核心-后端分离架构源于一个单一的、务实的动机：<strong>关系型数据库共享关系模型，但在细节上各不相同</strong>。后端抽象层是管理这种张力的经过验证的方式。" },{ tag: "ul", items: ["后端是解耦的独立模块——ActiveRecord 是用户，而非拥有者","通用表达式系统提供可移植的查询语义","扩展点让后端可以暴露数据库特有的能力","契约测试保证了所有后端的行为一致性","内置 SQLite 后端既是零配置默认项，也是参考实现"]},{ tag: "hr" },{ tag: "next", html: "下一篇预告：字段代理系统——如何在 Pydantic 字段之上叠加 ORM 特有的自定义行为。" }]})})();