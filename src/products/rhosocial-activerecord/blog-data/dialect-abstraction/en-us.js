(function(){var w=window.I18N;var d=w&&w['en-us'];if(!d)return;Object.assign(d,{hero:{ back: "Back to Blog", category:"Dialect · Extension",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"}, title: "Dialect System: Common Expressions & Dialect Extensions", sub: "Design of the common expression layer; how the dummy backend validates dialect correctness; how each backend extends its own dialect." },body:[ { tag: "p", html: "<strong>2026-05-20</strong> · by rhosocial team" }, { tag: "lead", html: "The Dialect system is rhosocial-activerecord's core mechanism for supporting multiple database backends. Its job is to receive common expression trees and translate them into target database-specific SQL. This article details the Dialect system's architecture and extension model." }, { tag: "h2", html: "Dialect Responsibilities" }, { tag: "p", html: "The Dialect system sits between the Expression system and the Connection Manager. Its core responsibilities include:" }, { tag: "ul", items: [ "<strong>Expression rendering</strong>: Translates common expression trees into database-specific SQL strings, handling all dialect differences", "<strong>Data type mapping</strong>: Maps rhosocial-activerecord/Pydantic data types to database native types (e.g., Python <code>int</code> → MySQL <code>INT</code> / PostgreSQL <code>INTEGER</code>)", "<strong>Function implementation</strong>: Provides database-specific function implementations (e.g., date functions, string functions)", "<strong>SQL syntax adaptation</strong>: Handles pagination syntax, identifier quoting (<code>\"</code> vs <code>`</code> vs <code>[]</code>), placeholder formats (<code>?</code> vs <code>%s</code> vs <code>$1</code>)", "<strong>DDL generation</strong>: Translates model definitions into CREATE TABLE / ALTER TABLE statements" ]}, { tag: "h2", html: "The Common Expression Layer" }, { tag: "p", html: "rhosocial-activerecord's core library defines a set of common expression classes. These are dialect-independent—they have the same semantics across all database backends. For example:" }, { tag: "code", text: "from rhosocial.activerecord.expressions import (\n Field, Literal, Comparison, Logical,\n Function, Subquery, OrderBy, Limit, Offset\n)" }, { tag: "p", html: "Each backend has a Dialect class that inherits from the core library's BaseDialect. The Dialect class is responsible for rendering these common expressions into database-specific SQL." }, { tag: "h2", html: "Dialect Extension Points" }, { tag: "p", html: "rhosocial-activerecord's Dialect system provides multiple layers of extension points, allowing each backend to customize as needed:" }, { tag: "h3", html: "1. Expression Renderer Registration" }, { tag: "p", html: "Each expression type can register a corresponding renderer. If a backend doesn't need special handling, it inherits BaseDialect's default renderer. If differentiated SQL output is needed, the backend can override the renderer for specific expression types:" }, { tag: "code", text: "class MySQLDialect(BaseDialect):\n def render_limit_offset(self, limit, offset, ctx):\n return f\"LIMIT {limit} OFFSET {offset}\"\n\nclass SQLServerDialect(BaseDialect):\n def render_limit_offset(self, limit, offset, ctx):\n return f\"OFFSET {offset} ROWS FETCH NEXT {limit} ROWS ONLY\"" }, { tag: "h3", html: "2. Type Mapping" }, { tag: "p", html: "Different databases have different naming and support for the same types. Dialect maintains a type mapping table that converts rhosocial-activerecord's common types to database native types:" }, { tag: "code", text: "class PostgreSQLDialect(BaseDialect):\n TYPE_MAP = {\n str: \"VARCHAR\", int: \"INTEGER\", float: \"DOUBLE PRECISION\",\n bool: \"BOOLEAN\", datetime: \"TIMESTAMPTZ\", date: \"DATE\",\n bytes: \"BYTEA\", dict: \"JSONB\", list: \"JSONB\",\n }\n\nclass MySQLDialect(BaseDialect):\n TYPE_MAP = {\n str: \"VARCHAR\", int: \"INT\", float: \"DOUBLE\",\n bool: \"TINYINT(1)\", datetime: \"DATETIME\", date: \"DATE\",\n bytes: \"BLOB\", dict: \"JSON\", list: \"JSON\",\n }" }, { tag: "h3", html: "3. Function Registration" }, { tag: "p", html: "Different databases have different function names and parameter rules. Dialect allows backends to register their own function implementations:" }, { tag: "code", text: "class PostgreSQLDialect(BaseDialect):\n FUNCTIONS = {\n \"NOW\": \"NOW()\", \"RANDOM\": \"RANDOM()\",\n \"CONCAT\": lambda args: f\"CONCAT_WS(' ', {', '.join(args)})\",\n \"GROUP_CONCAT\": lambda args: f\"STRING_AGG({args[0]}, ',')\",\n }\n\nclass MySQLDialect(BaseDialect):\n FUNCTIONS = {\n \"NOW\": \"NOW()\", \"RANDOM\": \"RAND()\",\n \"CONCAT\": lambda args: f\"CONCAT({', '.join(args)})\",\n \"GROUP_CONCAT\": lambda args: f\"GROUP_CONCAT({args[0]})\",\n }" }, { tag: "h2", html: "The Role of the Dummy Dialect" }, { tag: "p", html: "The Dummy Dialect is a reference implementation built into the core library. It does not connect to any database—it only validates the correctness of the Expression + Dialect system. The Dummy dialect's rendered output serves as a reference standard for all other dialects." }, { tag: "p", html: "When developing a new database backend, developers can:" }, { tag: "ul", items: [ "Inherit from BaseDialect and implement their own Dialect class", "Compare output against the Dummy dialect to verify correctness", "Run contract tests from testsuite to ensure all standard cases pass", "Gradually add backend-specific extensions (e.g., PostgreSQL JSONB, MySQL full-text indexes)" ]}, { tag: "h2", html: "Testing and Validation" }, { tag: "p", html: "rhosocial-activerecord's test suite (testsuite) provides standardized validation for the Dialect system:" }, { tag: "ul", items: [ "Each standard query case has a corresponding Expression tree", "The test framework renders the Expression tree using both Dummy dialect and target dialect", "Compares the structural equivalence of rendered output (not string equality—different dialects can produce different SQL, but semantics must match)", "After structural equivalence is verified, executes the query on the actual database to validate result correctness" ]}, { tag: "h2", html: "Summary" }, { tag: "p", html: "The Dialect system is the foundation of rhosocial-activerecord's multi-database support. Through extension points such as expression renderers, type mappings, and function registrations, each backend can flexibly adapt to its target database's characteristics while maintaining API consistency at the upper layer. The Dummy dialect and standardized test flow ensure this flexibility doesn't compromise correctness." }, { tag: "hr" }, { tag: "next", html: "Next: Named Connections—multi-database connection management." } ]})})();