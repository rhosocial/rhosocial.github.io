(function(){var w=window.I18N;var d=w&&w['zh-cn'];if(!d)return;Object.assign(d,{hero:{ back: "返回 Blog", category:"设计 · API",tools:{reportIssue:"反馈页面问题",copyLink:"复制链接"}, title: "Sync/Async API 对称设计：命名的艺术", sub: "为何同步异步方法名完全一致、类名加 Async 前缀。" },body:[ { tag: "p", html: "<strong>2026-05-18</strong> · by rhosocial team" }, { tag: "lead", html: "在 Python 异步生态中，一个常见的问题是：同步库和异步库的 API 往往不一致。AR 的设计原则是：<strong>同步和异步 API 的每个方法一一对应</strong>。开发者不需要学习两套 API——只需要决定使用同步还是异步版本。" }, { tag: "h2", html: "对称设计原则" }, { tag: "p", html: "AR 的对称设计体现在两个方面：" }, { tag: "ul", items: [ "<strong>方法名完全一致</strong>：同步的 <code>save()</code> 和异步的 <code>save()</code> 是同一个方法名——调用方式不同但语义相同", "<strong>类名加 Async 前缀</strong>：对应的异步版本类名以 <code>Async</code> 开头，如 <code>AsyncActiveRecord</code>、<code>AsyncActiveQuery</code>" ]}, { tag: "p", html: "这意味着开发者只需要知道 AR 的 API 设计，就可以同时使用同步和异步版本。从同步迁移到异步只是换一个类名的问题。" }, { tag: "h2", html: "同步 vs 异步" }, { tag: "code", text: "# 同步版本\nfrom rhosocial.activerecord import ActiveRecord\n\nclass User(ActiveRecord):\n id: int | None = None\n name: str\n email: str\n\n# 使用同步 API\nuser = User(name=\"Alice\", email=\"alice@example.com\")\nuser.save() # 同步调用\n\nusers = User.query().where(name__contains=\"Ali\").all() # 同步查询\n\n# 异步版本\nfrom rhosocial.activerecord import AsyncActiveRecord\n\nclass AsyncUser(AsyncActiveRecord):\n id: int | None = None\n name: str\n email: str\n\n# 使用异步 API\nuser = AsyncUser(name=\"Alice\", email=\"alice@example.com\")\nawait user.save() # 异步调用\n\nusers = await AsyncUser.query().where(name__contains=\"Ali\").all() # 异步查询" }, { tag: "p", html: "对比同步和异步的代码，唯一的区别是：异步版本需要 <code>await</code> 关键字，类名从 <code>ActiveRecord</code> 变为 <code>AsyncActiveRecord</code>。查询构建的链式调用方式完全一致。" }, { tag: "h2", html: "为什么不是统一 API" }, { tag: "p", html: "有人可能会问：为什么不做一个统一的 API，自动检测运行环境是同步还是异步？AR 团队评估过这个方案，决定放弃的原因包括：" }, { tag: "ul", items: [ "Python 的同步/异步不能透明互操作——同步代码里不能 <code>await</code>，异步代码里不能直接调用同步 IO 操作", "自动检测会增加运行时的复杂度和不可预测性——开发者应该明确知道自己在使用同步还是异步", "显式优于隐式——Async 前缀让使用异步版本的意图一目了然", "类型检查工具（mypy、pyright）对同步和异步的检测规则不同，统一的 API 会导致类型标注困难" ]}, { tag: "h2", html: "类名对应关系" }, { tag: "p", html: "AR 的同步和异步类名遵循一致的映射规则——在同步类名前加 <code>Async</code> 前缀：" }, { tag: "ul", items: [ "<code>ActiveRecord</code> → <code>AsyncActiveRecord</code>", "<code>ActiveQuery</code> → <code>AsyncActiveQuery</code>", "其他 Manager/Builder 类同样遵循此规则" ]}, { tag: "p", html: "这种命名约定让开发者可以轻松找到对应的异步版本。在 IDE 中输入 <code>Async</code> 即可看到所有可用的异步类。" }, { tag: "h2", html: "模型定义的复用" }, { tag: "p", html: "一个常见问题是：同步和异步版本是否需要定义两套模型？答案是<strong>不需要</strong>。" }, { tag: "p", html: "模型字段定义与同步/异步无关——Pydantic 的 <code>BaseModel</code> 没有同步/异步的概念。模型定义可以共享，只是在使用时选择同步或异步的数据库操作类：" }, { tag: "code", text: "# 共享模型定义\nclass User: \n id: int | None = None\n name: str\n email: str\n\n# 同步使用时继承 ActiveRecord\nclass SyncUser(User, ActiveRecord):\n pass\n\n# 异步使用时继承 AsyncActiveRecord\nclass AsyncUser(User, AsyncActiveRecord):\n pass" }, { tag: "p", html: "或者更常见的方式：直接在 ActiveRecord 上定义模型，然后在使用异步时切换。底层实现中，同步和异步版本共享同一个 Pydantic 字段系统。" }, { tag: "h2", html: "测试与调试" }, { tag: "p", html: "对称设计也简化了测试和调试。" }, { tag: "ul", items: [ "开发者可以在开发阶段使用同步版本（更易于调试、设置断点），在生产环境切换到异步版本", "测试用例可以在同步和异步之间轻松切换——同一个测试逻辑只需要换类名和加 await", "日志和错误信息在同步和异步版本中保持一致——排查问题时不需要在两种调用方式之间做心智切换" ]}, { tag: "h2", html: "总结" }, { tag: "p", html: "\"方法名一致，类名加 Async 前缀\"——这个看似简单的命名约定背后，是 AR 对开发者体验的深度关注。它让同步和异步之间的迁移成本降至最低，让团队可以在同一个代码库中混合使用同步和异步代码而不会产生认知负担。" }, { tag: "hr" }, { tag: "next", html: "下一篇：Expression 系统的设计哲学——为什么选择表达式树而非字符串拼接来生成 SQL。" } ]})})();