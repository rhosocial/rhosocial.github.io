(function(){var w=window.I18N;var d=w&&w['en-us'];if(!d)return;Object.assign(d,{hero:{category:"Design · API",tools:{reportIssue:"Report Issue",copyLink:"Copy Link"},author:"by rhosocial team"},body:[ { tag: "lead", html: "A common problem in Python's async ecosystem is that synchronous and asynchronous libraries often have inconsistent APIs. rhosocial-activerecord's design principle is simple: <strong>every method in synchronous and asynchronous APIs has a one-to-one correspondence</strong>. Developers don't need to learn two API sets—they only need to decide whether to use sync or async." }, { tag: "h2", html: "Symmetry Principles" }, { tag: "p", html: "rhosocial-activerecord's symmetry is reflected in two aspects:" }, { tag: "ul", items: [ "<strong>Identical method names</strong>: Synchronous <code>save()</code> and asynchronous <code>save()</code> share the same name—different calling conventions but same semantics", "<strong>Async-prefixed class names</strong>: Async versions use the <code>Async</code> prefix, e.g., <code>AsyncActiveRecord</code>, <code>AsyncActiveQuery</code>" ]}, { tag: "p", html: "This means developers only need to know rhosocial-activerecord's API design to use both sync and async versions. Migrating from sync to async is simply a matter of changing the class name." }, { tag: "h2", html: "Sync vs Async" }, { tag: "code", text: "# Synchronous version\nfrom rhosocial.activerecord import ActiveRecord\n\nclass User(ActiveRecord):\n id: int | None = None\n name: str\n email: str\n\n# Using sync API\nuser = User(name=\"Alice\", email=\"alice@example.com\")\nuser.save() # sync call\n\nusers = User.query().where(name__contains=\"Ali\").all()\n\n# Asynchronous version\nfrom rhosocial.activerecord import AsyncActiveRecord\n\nclass AsyncUser(AsyncActiveRecord):\n id: int | None = None\n name: str\n email: str\n\n# Using async API\nuser = AsyncUser(name=\"Alice\", email=\"alice@example.com\")\nawait user.save() # async call\n\nusers = await AsyncUser.query().where(name__contains=\"Ali\").all()" }, { tag: "p", html: "Comparing sync and async code, the only differences are: the <code>await</code> keyword and the class name changing from <code>ActiveRecord</code> to <code>AsyncActiveRecord</code>. The query chaining is identical." }, { tag: "h2", html: "Why Not a Unified API" }, { tag: "p", html: "One might ask: why not create a unified API that auto-detects whether the runtime is sync or async? The rhosocial-activerecord team evaluated this and decided against it for several reasons:" }, { tag: "ul", items: [ "Python's sync/async are not transparently interoperable—you can't <code>await</code> in sync code, and you can't call sync IO directly in async code", "Auto-detection adds runtime complexity and unpredictability—developers should know whether they're using sync or async", "Explicit is better than implicit—the Async prefix makes the intent clear at a glance", "Type checkers (mypy, pyright) have different detection rules for sync vs async—a unified API would make type annotation difficult" ]}, { tag: "h2", html: "Class Name Mapping" }, { tag: "p", html: "rhosocial-activerecord's sync and async class names follow a consistent mapping rule—adding the <code>Async</code> prefix:" }, { tag: "ul", items: [ "<code>ActiveRecord</code> → <code>AsyncActiveRecord</code>", "<code>ActiveQuery</code> → <code>AsyncActiveQuery</code>", "Other Manager/Builder classes follow the same rule" ]}, { tag: "p", html: "This naming convention makes it easy to find the corresponding async version. Typing <code>Async</code> in your IDE shows all available async classes." }, { tag: "h2", html: "Model Definition Reuse" }, { tag: "p", html: "A common question: do sync and async versions require defining two sets of models? The answer is <strong>no</strong>." }, { tag: "p", html: "Model field definitions are independent of sync/async—Pydantic's <code>BaseModel</code> has no concept of sync vs async. Model definitions can be shared; only the database operation class is chosen at usage time:" }, { tag: "code", text: "# Shared model definition\nclass User: \n id: int | None = None\n name: str\n email: str\n\n# Sync usage: inherit ActiveRecord\nclass SyncUser(User, ActiveRecord):\n pass\n\n# Async usage: inherit AsyncActiveRecord\nclass AsyncUser(User, AsyncActiveRecord):\n pass" }, { tag: "p", html: "Under the hood, sync and async versions share the same Pydantic field system." }, { tag: "h2", html: "Testing and Debugging" }, { tag: "p", html: "Symmetric design also simplifies testing and debugging." }, { tag: "ul", items: [ "Developers can use the sync version during development (easier debugging, breakpoint-friendly) and switch to async in production", "Test cases can be easily switched between sync and async—same test logic, just change class names and add await", "Logs and error messages are consistent between sync and async versions—no mental context switching when debugging" ]}, { tag: "h2", html: "Summary" }, { tag: "p", html: "\"Identical method names, Async-prefixed classes\"—behind this seemingly simple naming convention lies rhosocial-activerecord's deep commitment to developer experience. It minimizes the migration cost between sync and async, and lets teams mix sync and async code in the same codebase without cognitive burden." }, { tag: "hr" }, { tag: "next", html: "Next: The Design Philosophy of the Expression System—why expression trees over string concatenation for SQL generation." } ]})})();