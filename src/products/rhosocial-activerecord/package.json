{
  "name": "rhosocial-activerecord",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "rm -rf public/assets && cp -r assets public/assets",
    "dev": "astro dev",
    "postdev": "rm -rf public/assets",
    "prebuild": "rm -rf public/assets && cp -r assets public/assets",
    "build": "astro build && node scripts/postbuild.mjs",
    "postbuild": "rm -rf public/assets",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.3.1"
  }
}