(function(){var w=window.I18N;var d=w&&w['en-us'];if(!d)return;Object.assign(d,{hero:{back:"Back to Blog",eyebrow:"2026-05-14 · Design · API",title:"Fluent API: ActiveRecord.save and ActiveQuery",sub:"Making code read like English—the design philosophy and practice of fluent APIs."},body:[{tag:"p",html:"<strong>2026-05-14</strong> · by rhosocial team"},{tag:"lead",html:"A good API should feel like a natural language—fluent to read, intuitive to write. rhosocial ActiveRecord's fluent API is designed around this philosophy. <code>user.save()</code> reads as \"the user saves itself\", <code>User.query().where(...).order_by(...).all()</code> reads as \"query users where conditions met, ordered by field, return all\". This article explores the design philosophy and key practices of AR's fluent API."},{tag:"h2",html:"ActiveRecord.save(): Model as Data Source"},{tag:"p",html:"AR's core API is the <code>save()</code> method on model instances. It encapsulates CRUD operations as method calls on model objects, letting code express intent rather than implementation details:"},{tag:"code",text:"# Create\nuser = User(name=\"Alice\", email=\"alice@example.com\")\nuser.save()\n\n# Update\nuser.name = \"Bob\"\nuser.save()\n\n# Delete\nuser.delete()"},{tag:"p",html:"This design keeps business code focused on \"what to do\" rather than \"how to do it\". Developers don't need to worry about the SQL syntax differences between INSERT and UPDATE—AR automatically determines the right operation based on model state: new objects get INSERT, persisted objects get UPDATE."},{tag:"h2",html:"Batch Operations"},{tag:"p",html:"Beyond single-record save, AR provides batch operation APIs:"},{tag:"code",text:"# Bulk create\nUser.bulk_create([\n    User(name=\"Alice\"),\n    User(name=\"Bob\"),\n])\n\n# Bulk update\nUser.query().where(age__lt=18).update(status=\"minor\")\n\n# Bulk delete\nUser.query().where(status=\"inactive\").delete()"},{tag:"p",html:"Batch operations complete in a single database round-trip, significantly improving performance. Each backend can leverage its database's native bulk syntax (like PostgreSQL's <code>INSERT ... ON CONFLICT</code>) for further optimization."},{tag:"h2",html:"ActiveQuery: Building Queries"},{tag:"p",html:"ActiveQuery is AR's fluent query builder. Its goal is to make query code read like English sentences:"},{tag:"code",text:"users = User.query()\\\n    .where(age__gte=18)\\\n    .where(status=\"active\")\\\n    .order_by(\"-created_at\")\\\n    .limit(10)\\\n    .all()"},{tag:"p",html:"Each method returns <code>self</code> (or a new query object), supporting method chaining. Queries are actually executed only at <code>.all()</code>—this Lazy Evaluation design has two benefits:"},{tag:"ul",items:["Developers can compose query conditions incrementally instead of passing all parameters at once","AR can optimize the entire query before execution, merging conditions and removing redundant clauses"]},{tag:"h2",html:"Reading Query Methods"},{tag:"p",html:"AR's query method names follow English grammar principles, making each method call read like a natural language phrase:"},{tag:"ul",items:["<strong>.where(condition)</strong> — \"where the condition is...\", specifies filter criteria","<strong>.order_by(field)</strong> — \"ordered by...\", specifies sort field (prefix <code>-</code> for descending)","<strong>.limit(n)</strong> — \"limit to n\", restricts result count","<strong>.offset(n)</strong> — \"skip n\", pagination offset","<strong>.all()</strong> — \"all\", executes query and returns all results","<strong>.first()</strong> — \"first\", returns the first result or None","<strong>.one()</strong> — \"exactly one\", expects exactly one result or raises an exception","<strong>.count()</strong> — \"count\", returns the number of matching records","<strong>.exists()</strong> — \"exists\", returns a boolean"]},{tag:"p",html:"Combined: <code>User.query().where(age__gte=18).order_by(\"name\").limit(10).all()</code> reads as \"query users where age ≥ 18, ordered by name, limit 10, return all\"."},{tag:"h2",html:"Query Operators"},{tag:"p",html:"AR's query conditions use double-underscore operators to express semantics, a style familiar to Django ORM users:"},{tag:"code",text:"# Exact match\nUser.query().where(name=\"Alice\")\n\n# Comparison operators\nUser.query().where(age__gt=18)      # >\nUser.query().where(age__gte=18)     # >=\nUser.query().where(age__lt=65)      # <\nUser.query().where(age__lte=65)     # <=\nUser.query().where(age__ne=0)       # !=\n\n# Set operators\nUser.query().where(name__in=[\"Alice\", \"Bob\"])\nUser.query().where(name__not_in=[\"Charlie\"])\n\n# String matching\nUser.query().where(name__contains=\"Ali\")\nUser.query().where(name__startswith=\"A\")\nUser.query().where(name__endswith=\"ce\")\n\n# Null checks\nUser.query().where(email__isnull=True)"},{tag:"h2",html:"Chainability and Composability"},{tag:"p",html:"ActiveQuery's chaining supports free combination of conditions. Developers can build different query variants based on conditional branches:"},{tag:"code",text:"def search_users(name=None, min_age=None, active_only=False):\n    q = User.query()\n    if name:\n        q = q.where(name__contains=name)\n    if min_age:\n        q = q.where(age__gte=min_age)\n    if active_only:\n        q = q.where(status=\"active\")\n    return q.order_by(\"name\").all()"},{tag:"p",html:"This approach makes query construction more flexible and avoids the parameter explosion problem. Each query condition is optional and composable."},{tag:"h2",html:"Pydantic Integration"},{tag:"p",html:"AR's <code>save()</code> method triggers a full Pydantic field validation before executing the database operation. This means that even if you bypass type annotations and set field values directly, AR can catch data issues before persistence."},{tag:"p",html:"AR also supports <code>save(validate=False)</code> to skip validation, suitable for performance-sensitive or pre-validated scenarios."},{tag:"h2",html:"Summary"},{tag:"p",html:"AR's fluent API design follows three principles:"},{tag:"ul",items:["<strong>Readability first</strong>: Code should be readable like English, not a sequence of symbols requiring mental parsing","<strong>Lazy execution</strong>: Query construction and execution are separated, leaving room for optimization","<strong>Composability</strong>: Query conditions are freely combinable building blocks, not bundled parameter lists"]},{tag:"p",html:"These principles make AR's API both intuitive and flexible, suitable for everything from rapid prototyping to production-grade applications."},{tag:"hr"},{tag:"next",html:"Next: Relation Mapping—declarative belongs_to / has_many definitions and loading strategies."}]})})();