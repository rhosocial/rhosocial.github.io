(function(){var w=window.I18N;var d=w&&w['zh-cn'];if(!d)return;Object.assign(d,{hero:{ back: "返回 Blog", eyebrow: "2026-05-20 · Dialect · Extension", title: "Dialect 系统：通用表达式与方言扩展", sub: "通用表达式层的设计；dummy 演示后端如何验证方言系统的正确性；各后端如何根据自身特性扩展方言。" },body:[ { tag: "p", html: "<strong>2026-05-20</strong> · by rhosocial team" }, { tag: "lead", html: "Dialect（方言）系统是 AR 支持多数据库后端的核心机制。它的职责是：接收通用表达式树，将其翻译为目标数据库的 SQL 方言。这篇文章详细介绍 Dialect 系统的架构设计和扩展方式。" }, { tag: "h2", html: "Dialect 的职责" }, { tag: "p", html: "Dialect 系统位于 Expression 系统之下、Connection Manager 之上。它的核心职责包括：" }, { tag: "ul", items: [ "<strong>表达式渲染</strong>：将通用表达式树渲染为具体的 SQL 字符串，处理所有方言差异", "<strong>数据类型映射</strong>：将 AR/Pydantic 的数据类型映射为数据库的原生类型（如 Python <code>int</code> → MySQL 的 <code>INT</code> / PostgreSQL 的 <code>INTEGER</code>）", "<strong>函数实现</strong>：提供数据库特有的函数实现（如日期函数、字符串函数）", "<strong>SQL 语法适配</strong>：处理分页语法、标识符引用（<code>\"</code> vs <code>`</code> vs <code>[]</code>）、占位符格式（<code>?</code> vs <code>%s</code> vs <code>$1</code>）", "<strong>DDL 生成</strong>：将模型定义翻译为 CREATE TABLE / ALTER TABLE 等 DDL 语句" ]}, { tag: "h2", html: "通用 Expression 层" }, { tag: "p", html: "AR 的核心库定义了一套通用表达式类（Common Expressions）。这些表达式类是方言无关的——它们在所有数据库后端上语义相同。例如：" }, { tag: "code", text: "from rhosocial.activerecord.expressions import (\n Field, Literal, Comparison, Logical,\n Function, Subquery, OrderBy, Limit, Offset\n)" }, { tag: "p", html: "每个后端都有一个 Dialect 类，继承自核心库的 BaseDialect。Dialect 类负责将这些通用表达式渲染为特定数据库的 SQL。" }, { tag: "h2", html: "Dialect 的扩展点" }, { tag: "p", html: "AR 的 Dialect 系统设计了多层扩展点，允许每个后端按需定制：" }, { tag: "h3", html: "1. 表达式渲染器注册" }, { tag: "p", html: "每个表达式类型都可以注册一个对应的渲染器。如果后端不需要特殊处理，会使用 BaseDialect 的默认渲染器。如果需要差异化的 SQL 输出，后端可以覆盖特定表达式类型的渲染器：" }, { tag: "code", text: "class MySQLDialect(BaseDialect):\n def render_limit_offset(self, limit, offset, ctx):\n \"\"\"MySQL 使用 LIMIT ? OFFSET ? 语法\"\"\"\n return f\"LIMIT {limit} OFFSET {offset}\"\n\nclass SQLServerDialect(BaseDialect):\n def render_limit_offset(self, limit, offset, ctx):\n \"\"\"SQL Server 使用 OFFSET ... FETCH NEXT 语法\"\"\"\n return f\"OFFSET {offset} ROWS FETCH NEXT {limit} ROWS ONLY\"" }, { tag: "h3", html: "2. 类型映射" }, { tag: "p", html: "不同数据库对同一类型的命名和支持不同。Dialect 维护一个类型映射表，将 AR 的通用类型转换为数据库原生类型：" }, { tag: "code", text: "# PostgreSQL Dialect 类型映射示例\nclass PostgreSQLDialect(BaseDialect):\n TYPE_MAP = {\n str: \"VARCHAR\",\n int: \"INTEGER\",\n float: \"DOUBLE PRECISION\",\n bool: \"BOOLEAN\",\n datetime: \"TIMESTAMPTZ\",\n date: \"DATE\",\n bytes: \"BYTEA\",\n dict: \"JSONB\",\n list: \"JSONB\",\n }\n\n# MySQL Dialect 类型映射示例\nclass MySQLDialect(BaseDialect):\n TYPE_MAP = {\n str: \"VARCHAR\",\n int: \"INT\",\n float: \"DOUBLE\",\n bool: \"TINYINT(1)\",\n datetime: \"DATETIME\",\n date: \"DATE\",\n bytes: \"BLOB\",\n dict: \"JSON\",\n list: \"JSON\",\n }" }, { tag: "h3", html: "3. 函数注册" }, { tag: "p", html: "不同数据库的函数命名和参数规则各异。Dialect 允许后端注册自己的函数实现：" }, { tag: "code", text: "class PostgreSQLDialect(BaseDialect):\n FUNCTIONS = {\n \"NOW\": \"NOW()\",\n \"RANDOM\": \"RANDOM()\",\n \"CONCAT\": lambda args: f\"CONCAT_WS(' ', {', '.join(args)})\",\n \"GROUP_CONCAT\": lambda args: f\"STRING_AGG({args[0]}, ',')\",\n }\n\nclass MySQLDialect(BaseDialect):\n FUNCTIONS = {\n \"NOW\": \"NOW()\",\n \"RANDOM\": \"RAND()\",\n \"CONCAT\": lambda args: f\"CONCAT({', '.join(args)})\",\n \"GROUP_CONCAT\": lambda args: f\"GROUP_CONCAT({args[0]})\",\n }" }, { tag: "h2", html: "Dummy 方言的作用" }, { tag: "p", html: "Dummy 方言是核心库内置的一个\"参考实现\"。它不连接任何数据库，只用于验证 Expression + Dialect 系统的正确性。Dummy 方言的渲染结果作为其他所有方言的参照标准。" }, { tag: "p", html: "开发一个新的数据库后端时，开发者可以：" }, { tag: "ul", items: [ "继承 BaseDialect，实现自己的 Dialect 类", "对照 Dummy 方言的输出，验证自己的方言是否正确", "运行 testsuite 中的契约测试，确保所有标准用例通过", "逐步添加后端特有的功能扩展（如 PostgreSQL 的 JSONB、MySQL 的全文索引）" ]}, { tag: "h2", html: "测试验证流程" }, { tag: "p", html: "AR 的测试套件（testsuite）针对 Dialect 系统提供了一套标准化的验证流程：" }, { tag: "ul", items: [ "每个标准查询用例有一个对应的 Expression 树", "测试框架将该 Expression 树分别用 Dummy 方言和目标方言渲染", "对比渲染结果的结构等价性（不是字符串相等——不同方言的 SQL 可以不同，但语义必须相同）", "结构等价性验证通过后，在实际数据库上执行查询验证结果正确性" ]}, { tag: "h2", html: "总结" }, { tag: "p", html: "Dialect 系统是 AR 支持多数据库的基石。它通过 Expression 渲染器、类型映射、函数注册等扩展点，让各个后端可以灵活地适应目标数据库的特性，同时保持上层 API 的一致性。Dummy 方言和标准化测试流程确保了这种灵活性不会牺牲正确性。" }, { tag: "hr" }, { tag: "next", html: "下一篇预告：命名连接——多数据库连接管理。" } ]})})();